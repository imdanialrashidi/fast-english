# Secrets and local configuration
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# Android signing material and release artifacts (P4-S2 signing-key gate).
# Production keystores, passwords and signing properties must never be
# committed; passwords come from the environment or an ignored local file.
releases/
keystore.properties
keystore.properties.*
*.storepass
*.keypass

# Confidential project/client material
docs/private/**
!docs/private/.gitkeep

# Pi local runtime and installed project packages
.pi/npm/
.pi/git/
.pi/sessions/
.pi/cache/
.pi/auth.json
.pi/models.json
.pi/trust.json
.pi/mcp.json
.pi/mcp-oauth/
.pi/mcp-traces/

# Dependencies and environments
node_modules/
.pnpm-store/
.venv/
venv/
vendor/

# Build, test, browser, and framework output
dist/
build/
coverage/
target/
.next/
.turbo/
.vite/
dist-app/
dist-landing/
dist-landing-apk/
dist-admin/
.artifacts/
playwright-report/
test-results/
*.log

# PocketBase
server/pb_data/
server/pocketbase
server/pocketbase.exe

# Editors / OS
.idea/
.vscode/
.DS_Store
