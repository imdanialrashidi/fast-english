// app/src/features/payment/index.ts
// Public entry point for the payment feature. Routes are imported by
// the app's router; everything else is internal to the feature.

export { PaymentRoute } from './routes/PaymentRoute';
export { PaymentStatusRoute } from './routes/PaymentStatusRoute';
