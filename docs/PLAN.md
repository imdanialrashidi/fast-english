# Current Implementation Plan — Fast English Podcast MVP

> Status note: this file is the historical slice-by-slice implementation log.
> The "Facts, constraints, and assumptions" section describes the starting
> scaffold and is deliberately not rewritten as slices land. For the current
> authoritative state, use `docs/PRODUCT.md` (product contract),
> `docs/ARCHITECTURE.md` (boundaries/invariants), `docs/DESIGN.md` (accepted
> visual direction), and `docs/QUALITY.md` (verification lanes and gates).
> `scripts/project-verify.sh` is the executable full backend gate; the
> canonical entry is `pnpm verify:full`.

## Goal and non-goals
- Goal: deliver the most complete verifiable MVP in five phases (0–4) by end of 2026-07-30: Persian-first English-learning product with manual payment, operator review, placement, lessons+audio, PWA + APK, static landing.
- Non-goals: CEFR certification, SMS OTP, email verification, self-service password recovery, payment/landing crossover, CMS, SSR, GraphQL, microservices, Docker, Next.js/Astro.

## Facts, constraints, and assumptions
- Repo is a clean scaffold on `main` (commit 6e9f939); no app code, no lockfile, no `server/`/`android/`.
- Local: Node v22.23.1, pnpm 11.3.0, adb 1.0.41. CI: Node 24.
- `.gitignore` must add `server/pb_data/` (currently only watcher/permission-denied, not ignored).
- PocketBase `PasswordAuth.IdentityFields` is configurable → `phone` is a native identity field (Context7-verified).
- Exact dependency versions pinned at Phase 0 via Context7 + generated lockfile (no lockfile exists yet).
- Assumption: package ID `com.fastenglishpodcast.app` (reversible).
- Assumption: receipt max 5 MB unless repo/deploy evidence requires lower.
- Hard deadline = prioritization constraint; security invariants never downgraded.

## Existing components/patterns to reuse
- `scripts/verify.sh` (dispatcher; will add `scripts/project-verify.sh`), `scripts/ci-install.sh`, `.github/workflows/quality.yml`.
- `opencode.jsonc` plan-agent edit allowlist for docs; `risk-review` skill for `/review`.
- Workflow: `/plan` → `/build` (one slice/session) → `/review` (sensitive) → `/ship`.

## Smallest viable design and data/control flow
- Single pnpm package, two Vite configs (`vite.landing.config.ts`, `vite.app.config.ts`), outputs `dist-landing/` + `dist-app/`; Capacitor `webDir=dist-app`.
- PocketBase: `users` auth collection (phone identity), `plans`, `payment_requests`, `subscriptions`, `placement_attempts`, `placement_questions` (versioned), `topics`, `lessons`, `progress`; JS migrations + hooks for authz/transactions/grading.
- Env-aware API-base resolver (browser origin / Android explicit HTTPS / dev proxy / adb reverse).

## Risks and controls
- Security: client tampering of role/price/status → server ignores client-set protected fields; tests for tampering.
- Payment idempotency: double approval → unique subscription→request link + transaction; test repeated approval.
- Receipt privacy: public URL leak → protected file field, randomized name, no logs; test unauthorized access.
- Placement answer leakage: client never receives correct answers; test 20-question flow + duplicate submit.
- Premium denial: direct API by pending/expired → server-side check; test deny/allow.
- APK origin confusion: bundled APK ≠ browser origin → explicit HTTPS API base; test device connectivity.
- Two-day feasibility: content (lessons/questions/audio) and prices are external inputs → fixtures for dev/test only; mark unverified items in final report.

## Ordered slices

### Phase 0 — Visual foundation and walking skeleton
- [x] **P0-S1 — Bootstrap & build topology** (req: ARCH topology, verify contract). Outcome: reproducible two-surface Vite build + verify scripts. In: root `package.json`, `pnpm-lock.yaml`, `tsconfig`, Biome, Vitest, `vite.landing.config.ts`, `vite.app.config.ts`, `scripts/project-verify.sh`, `.gitignore` += `server/pb_data/`, `server/VERSION`. Out: product UI. Files: ~8. Schema/API: none. UI states: none. Security: no secrets in bundle. Verify: `pnpm typecheck/check/build`, `scripts/verify.sh`. Accept: both builds emit isolated `dist-*`; verify green. Deps: none. Inputs: none. Rollback: delete generated files. Budget: 1 dep set. /review: no.
  - **Implemented:** Node 24 LTS (`.nvmrc`, `engines.node >=24 <25`); pnpm 11.17.0 (`packageManager` + `engines.pnpm >=11 <12`; local install used pnpm 11.17.0 via corepack-equivalent resolution — `corepack` binary not present in this environment, so system pnpm 11.17.0 was used directly; lockfile is pnpm 11.x format). Pinned: react/react-dom 19.2.8, @types/react 19.2.17, @types/react-dom 19.2.3, @types/node 22.20.1, typescript 7.0.2, vite 8.1.5, @vitejs/plugin-react 6.0.4, @biomejs/biome 2.5.6, vitest 4.1.10. PocketBase `server/VERSION` = 0.39.9 (binary NOT downloaded). pnpm 11 auto-creates a `pnpm-workspace.yaml` with `minimumReleaseAgeExclude` for newly-released packages; this was moved into `package.json` under `pnpm` field to honor the "no workspace" rule. No Playwright dep added (deferred to P2/P4 when actually needed). Zero behavioral tests in this slice; `pnpm test` uses `vitest run --passWithNoTests`. Local verification ran on Node 22.23.1 (pinned Node 24 not activatable without corepack/nvm); all tools officially support Node ≥22.12, so the gate is honest. CI uses `actions/setup-node@v7` with `node-version: 24` and `corepack enable` (provided by ci-install.sh) — CI is the authoritative Node 24 run.
- [x] **P0-S2 — MUI RTL theme, Vazirmatn, app shell, landing foundation** (req: UI/visual). Outcome: premium RTL shell + nav observable at QA widths; landing skeleton. In: `app/theme`, Vazirmatn WOFF2 + license note, `app/App`, bottom nav + larger-screen nav, route stubs, `landing/` skeleton + brand tokens. Out: backend behavior. Files: ~8. UI states: loading/empty skeletons. Security: none. Verify: typecheck/build + Playwright screenshots at 360/375/390/430/768/1440. Accept: RTL correct (dialogs/menus), no horizontal overflow, Vazirmatn loads locally. Deps: MUI, Emotion RTL, React Router. Inputs: logo/icons (placeholder ok, flagged). Rollback: revert theme. Budget: 1 dep (MUI). /review: no.
  - **Implemented:** MUI v9.2.0 (`@mui/material`, `@mui/icons-material`) with CSS theme variables (`createTheme({ cssVariables: true, direction: 'rtl' })`) and per-component defaults for MuiButton/Card/TextField/Chip/Dialog/Menu/Popover/Tooltip/BottomNavigation/AppBar/Drawer/Alert. Emotion 11.14 + Emotion RTL cache (`prefixer` + `stylis-plugin-rtl`) so MUI portals (Dialog/Menu/Popover/Tooltip/Drawer) inherit correct direction; portals also receive explicit `dir="rtl"` default. Vazirmatn v33.003 variable WOFF2 self-hosted under `app/src/assets/fonts/Vazirmatn-VF.woff2` with `OFL.txt` retained; no runtime CDN. React Router 8.3.0 declarative SPA; all 11 routes present. App shell: `AppShell` + `AppHeader` (midnight) + `StudentBottomNav` (mobile, safe-area) + `StudentSideNav` (md+, persistent Drawer) + `PageContainer` (bottom-pad for mobile nav). Shared: `BrandMark`, `PageHeader`, `StatePanel` (6 variants: loading/empty/error/permission/offline/unavailable; always icon+text), `LevelBadge` (CEFR token, icon+text). Representative screens: entry, login, signup, dashboard, payment, payment-status, placement, lessons, lesson demo (LTR English body, width-bounded 38rem), account, operator (no student bottom nav). Deterministic preview data in `app/src/data/previewData.ts`, clearly marked. Landing (Tailwind v4.3.3 via `@tailwindcss/vite` plugin **only** in `vite.landing.config.ts`): 7 sections (Header, Hero with midnight CEFR-stripe visual + waveform, CefrSection, SampleLesson, HowItWorks, CtaSection, Footer). `@import "tailwindcss"` + `@source` restricted to `landing/**/*.{ts,tsx,html}`. Vazirmatn duplicated to `landing/src/assets/fonts/` for full build isolation. Tests: 35 passing across 6 files (theme RTL+CEFR+vars+Vazirmatn; routes declared; nav labels + responsive breakpoints; lesson LTR; landing sections + Tailwind import + source restriction + font self-host; build boundary isolation — no Tailwind in app, no MUI in landing, no cross-surface imports). Two visual passes: first pass found LevelBadge chip/text had no visible gap (MUI v9 Stack `spacing` does not consistently apply when direction is set via `sx`); correction pass rewrote LevelBadge with `Box` + `display: inline-flex` + `gap: 12px` — gap now clearly visible in dashboard, lesson header, and recommendation cards. `noImportantStyles` Biome rule disabled (legitimate `!important` in reduced-motion override). `css.parser.tailwindDirectives: true` added to Biome so Tailwind v4 `@source`/`@theme` directives parse. `biome check --write` auto-fixed import sort. Local Node 22.23.1 (pinned 24 via `.nvmrc`/`engines`); all tools support Node ≥22.12 so gate is honest. CI unchanged — `actions/setup-node@v7` already on Node 24 with `corepack enable`.
- [x] **P0-S3 — PocketBase skeleton + phone auth + session restore + API-base + debug APK** (req: auth, API topology, Android). Outcome: real PB signup/login in browser + debug APK reaches dev backend. In: `server/pb_migrations` (users auth collection, phone identity, role/account_status defaults), phone normalization hook, `app/api` env-aware resolver, auth pages, session restore, Capacitor init, debug build. Out: payment/placement/lessons. Files: ~8. Schema: `users`. API: `auth-with-password`, create user. UI states: loading/error/disabled/permission-denied. Security: client never sets role/status; phone canonical uniqueness; tampering test. Verify: real PB smoke (valid/invalid signup/login, phone collision, session restore, client role/status tamper), `adb install` + first launch. Accept: browser + device signup/login work against real PB; no mock mistaken for backend. Deps: PocketBase binary, Capacitor. Inputs: physical Android device. Rollback: drop migration. Budget: 1 sensitive endpoint. /review: YES (auth).
  - **Implemented:** PocketBase 0.39.9 downloaded to `server/pocketbase` (gitignored) via `scripts/setup-pocketbase.sh` from the official GitHub release. Users auth collection `fep_users` created by `server/pb_migrations/1700000000_create_users.js` with: `name`, `phone` (unique indexed), `role` (student/operator/content_manager), `account_status` (pending_payment/payment_rejected/active/expired/suspended), `placement_completed`, `suggested_level`, `selected_level`, `suspended_reason`; `passwordAuth.identityFields = ["email"]` (PB 0.39 forces email into the list, see note). API rules: `listRule=viewRule="id=@request.auth.id"`, `createRule=""` (public signup), `updateRule="id=@request.auth.id"`, `deleteRule=""`. Hooks in `server/pb_hooks/main.pb.js`: model hook `onRecordCreate` normalizes phone to `+989XXXXXXXXX` and overwrites protected fields with safe defaults (`role=student`, `account_status=pending_payment`, `placement_completed=false`); model hook `onRecordUpdate` blocks student self-mutation of protected fields; request hooks `onRecordAuthRequest` / `onRecordAuthRefreshRequest` block suspended users. Hook helpers are inlined (PB 0.39 JSVM does not share top-level functions with hook scopes). Phone login uses `phoneToInternalEmail()` client-side to derive `<canonical_phone>@fep.local` and `pb.collection('fep_users').authWithPassword(email, password)` — the user-facing identity is still phone. API origin resolver at `app/src/lib/apiOrigin.ts`: browser = `window.location.origin`, Android debug = `VITE_ANDROID_API_ORIGIN` (validated), Android release = hard-coded `https://app.fastenglishpodcast.com` (rejects loopback/HTTP in production). Single PocketBase client at `app/src/lib/pocketbase.ts`. AuthProvider at `app/src/lib/auth.tsx` with `signup`/`login`/`logout`/`refresh`, `authRefresh()` on init, `onChange` subscription, route guards `decideRoute` (guest-only/pending-only/active-only/operator-only). RHF+Zod forms for Signup and Login; recovery support Dialog (real MUI portal, RTL, focus, Escape). Tests: 73 passing across 10 files (phone normalization 11 cases, auth guards 15 cases, error mapping 9 cases, build boundary, route presence, nav labels). Real-backend smoke `scripts/smoke-auth.mjs` (run via `scripts/smoke-auth.sh`): health, collection presence, signup 09.../Persian/989.../invalid/duplicate/empty/email/role-tampering, login correct/wrong/alternate-form, authRefresh valid/invalid, list restriction, protected-field update. **Note: PB 0.39 forces `email` into `passwordAuth.identityFields` for any auth collection** — setting `identityFields=["phone"]` via the migration is reverted by PB on save. The pragmatic resolution: phone is the user-facing identity, the hook derives the internal email from the canonical phone, and the client SDK authenticates with that derived email. Documented in `docs/ARCHITECTURE.md` and the hook comments. Capacitor 8.4.2 initialized: `capacitor.config.json` (appId `com.fastenglishpodcast.app`, appName "Fast English Podcast", webDir `dist-app`, no `server.url`). `android/` generated and committed. Debug-only cleartext in `android/app/src/debug/res/xml/network_security_config.xml` for `localhost`/`127.0.0.1`; release manifest has no cleartext. APK built: `android/app/build/outputs/apk/debug/app-debug.apk` (4.3 MB), bundles `dist-app/` assets, no `server.url`, no secrets. Physical-device validation: `NOT RUN` — no authorized Android phone connected via `adb devices`. Commands documented for manual device check: `adb reverse tcp:8090 tcp:8090 && adb install -r app-debug.apk`. CI: Android compilation not in default `scripts/verify.sh` (separate gate per task). Dependencies pinned: `pocketbase@0.27.0`, `react-hook-form@7.83.0`, `zod@4.4.3`, `@hookform/resolvers@5.5.7`, `@capacitor/{core,cli,android}@8.4.2`, `@playwright/test@1.62.0` (installed, E2E deferred to a follow-up because no browser binary is available — system Chromium used as the closest honest alternative for non-auth visual checks). `pnpm-workspace.yaml` auto-generation handled by keeping the `pnpm` config block inside `package.json` (no workspace file committed). Local Node 22.23.1 vs pinned 24 documented; all tools officially support Node ≥22.12.

### Phase 1 — Manual payment and operator control
- [x] **P1-S1 — Plans, payment request, protected receipt, resubmit** (req: payment, receipt). Outcome: user submits one protected receipt; sees pending/rejected; resubmits after rejection. In: `plans`, `payment_requests` migrations; plan snapshot; receipt upload (JPEG/PNG/WebP ≤5MB, signature/MIME/ext check, randomized name, protected field); one-pending-per-user; resubmit-after-rejection; pending/rejected UI. Out: operator review. Files: ~8. Schema: `plans`, `payment_requests` (+ receipt file). API: create/list/get request, upload receipt. UI states: loading/empty/error/disabled/success. Security: client submits only plan_id + permitted fields; size/type/signature validation; no public URL; no URL in logs; tampering test (amount/status). Verify: real PB smoke (invalid size/type/signature, unauthorized receipt access, one-pending, tamper). Accept: one disposable receipt stored protected; unauthorized access fails. Deps: none new. Inputs: plan prices (fixture until provided). Rollback: migration down. Budget: 1 sensitive endpoint. /review: YES (payments/uploads).
  - **Implemented (P1-S1A backend):** Migrations `1700000002_create_plans.js` (listRule/viewRule=`is_active = true`; create/update/delete null; unique `slug`; required name/slug/duration_days/price_toman; non-required `is_active` so PATCH can deactivate; non-required `display_order`; description max 500), `1700000003_create_payment_destination.js` (singleton: listRule/viewRule=`is_active = true`; card_number/card_holder_name/bank_name required; instructions/support_contact/review_sla_text optional; non-required `is_active`), `1700000004_create_payment_requests.js` (list/view/create/update/delete null; relation `user`+`plan`; `plan_name_snapshot`/`amount_snapshot`/`duration_days_snapshot` text/number required; `receipt_file` protected FileField max 5MB, maxSelect 1, mimeTypes [image/jpeg,image/png,image/webp]; bank_reference ≤80; sender_card_last4 ≤4; transfer_at Date; status select pending/approved/rejected/cancelled required; public_rejection_reason ≤500; internal_note ≤1000; reviewed_by relation; reviewed_at Date; subscription text reserved; partial unique index `idx_payment_requests_one_pending_per_user` on user WHERE status='pending'). Custom route `server/pb_hooks/payment_routes.pb.js` exposes `POST /api/fast-english/payment-requests` and `GET /api/fast-english/payment-requests/current` (requireAuth `fep_users`). POST validates account state (suspended → 403 account_suspended; non-pending/rejected → 403 account_not_eligible), per-user rate limit (5/10min) inside the handler (PB 0.39's audience='@auth' is not honored for custom routes), parses multipart, validates plan_id/receipt_file presence + size ≤5MB (413 receipt_too_large), extension cross-check with byte signature (JPEG/PNG/WebP), declared Content-Type cross-check with signature, validates plan active + destination active (404 invalid_plan / payment_destination_unavailable), snapshots plan name/price/duration server-side, randomizes receipt filename (`$security.randomString(16).ext`), and creates the record in a transaction with a pending re-check. GET returns pending first, else most-recent rejected, approved, cancelled, else `{kind: "none"}`. Both routes return sanitized payloads: no `internal_note`, `reviewed_by`, raw `user` relation, or permanent receipt URLs. Migration `1700000005_extend_rate_limits_for_payment.js` adds a transport-level rate rule on `POST /api/fast-english/payment-requests` (100/10min, all audiences) so flooding is contained even when the per-user window is not. `scripts/smoke-payment.mjs` is a real-PB 23-scenario suite covering: unauthenticated 401, inactive plan 404, missing destination 404, missing receipt 400, two receipts 400, oversized 413, JPEG/PNG/WebP success (each in its own user), invalid signatures (PDF/SVG/ZIP/HTML/TXT) 400, MIME/signature mismatch (3 cases incl. a valid one), ext/signature mismatch 400, direct PB create blocked (403), direct PATCH/DELETE blocked (403), client snapshots ignored (status/amount/duration/plan_name from plan only), stored snapshots equal plan values, plan-change immutability, second pending 409 pending_request_exists, concurrent duplicate yields exactly one success, owner can read /current, another user cannot, internal_note absent from response, real HTTP 429 from the per-user window. The 23/23 suite passes.
  - **Implemented (P1-S1B backend hardening):** Migration `1700000005` adds the route-specific rate limit; hook handler in `server/pb_hooks/payment_routes.pb.js` enforces the per-user 5/10min window and returns Retry-After. `scripts/smoke-payment.mjs` scenario 23 fires 6 sequential attempts from a single user and asserts a real HTTP 429 (not a 400). Scenarios 13/14/15 prove client-supplied user/status/amount/duration/plan_name_snapshot are ignored and the stored snapshots equal the plan; scenario 16 proves one-pending-per-user; scenario 18 proves direct PB create is blocked; scenario 19 proves direct update/delete are blocked; scenario 20 proves /current surfaces the right kind; scenario 21 proves another user cannot read /current. 23/23 passing.
  - **Implemented (P1-S1C client UI):** `app/src/features/payment/` (feature-owned module) with `api.ts` (typed wrappers: `loadActivePlans`, `loadActiveDestination`, `loadCurrentRequest`, `createPaymentRequest` — only `plan_id`/`receipt_file`/`bank_reference`/`sender_card_last4`/`transfer_at` are sent; Content-Type header is left to the SDK to generate the boundary), `types.ts` (Plan / PaymentDestination / PaymentRequest / CurrentRequestResponse / CreateRequestResponse / PaymentError), `schemas.ts` (Zod wire-level + form-level: file size ≤5 MB, JPEG/PNG/WebP only, last-4 normalised from Persian/Arabic-Indic digits to Latin, transfer_at reject >24h future), `errors.ts` (safe Persian error mapper: 400/401/403/404/409/413/429/network → stable code + message; raw response never leaked), `formatters.ts` (Persian Toman via `fa-IR` locale thousands separator, last-four, card number 4-digit grouping with Arabic comma, duration, Persian date+time, file size), `useReceiptPreview.ts` (short-lived authorized token from `pb.files.getToken()`; URL built via `pb.files.getURL`; token never stored in localStorage/sessionStorage; nothing logged), `components/PlanSelector.tsx` (radiogroup, selected communicated by icon+border+aria-checked, keyboard-selectable), `components/DestinationCard.tsx` (formatted card number with Arabic comma, LTR numeric block, Copy action with brief confirmation), `components/ReceiptPicker.tsx` (single file picker, MIME/size client-side validation mirrors server, local Object URL preview, URL revoked on replace/removal/unmount, no Base64/localStorage/automatic upload), `components/StatusBadge.tsx` (icon+text; never color alone), `components/ReceiptPreview.tsx` (owner-only token-gated <img>; loading/error/retry states), `routes/PaymentRoute.tsx` (loads real plans+destination, redirects to /payment-status on existing pending, RHF+Zod form, double-submit prevention via `isSubmitting` + `submissionDisabled` gate, server error → form-field error mapping, on success clears local File+URL and navigates to /payment-status), `routes/PaymentStatusRoute.tsx` (none/pending/rejected/approved/cancelled states; approved is display-only with explicit "P1-S2 owns activation" copy; rejected offers a fresh submission path that creates a new request via the real route — never mutates the rejected record). App router at `app/src/app/App.tsx` imports the new feature via the feature's barrel and drops the placeholder route file. 9 focused test files: formatters, schemas, errors, api, api.dup (single-flight + forbidden-fields + status code mapping), ReceiptPicker.url (Object URL lifecycle), receiptPreview.contract (no storage / no logs / pb.files.getURL only), PaymentStatusRoute.approved (static guard: approved branch never touches subscription/account_status/placement/lessons). `npx tsc --noEmit` clean; `npx biome check .` 0 errors (14 pre-existing warnings in untouched files); `npx vitest run` 149/149 (was 73); `npx vite build` for both surfaces clean; `bash scripts/verify.sh` clean; `bash scripts/smoke-auth.sh` clean; `bash scripts/smoke-payment.sh` 23/23. Manual client integration check against disposable PB (11/11): plan+destination load with real values, signup+login, /current none, submission with Persian last-four (server normalises to Latin), /current after submit, second submit 409, refresh retains pending, owner file-token issuance (197 chars), no storage references in response. P1-S1 entirely complete; Phase 1 is NOT yet complete (P1-S2 remains).
  - **Implemented (P1-S1D secure owner-only receipt preview + browser E2E + responsive QA):** Replaced the `pb.files.getToken()` + `pb.files.getURL` client approach with a dedicated authenticated custom route `GET /api/fast-english/payment-requests/{requestId}/receipt` in `server/pb_hooks/payment_routes.pb.js`. The route (a) requires `fep_users` auth, (b) loads the Payment Request by Route parameter, (c) returns 404 for any non-owner, missing record, or missing receipt (no existence leak), (d) 403 for suspended accounts, (e) reads the bytes from `<dataDir>/storage/<collectionId>/<recordId>/<filename>` (no public URL, no file token), (f) re-derives Content-Type from the on-disk signature, (g) sets `X-Content-Type-Options: nosniff`, `Cache-Control: no-store`, and a safe inline `Content-Disposition`, (h) caps the read at 5 MB, and (i) never logs paths, bytes, or raw error details. The collection's `viewRule` remains `null` — opening it broadly (e.g. `user = @request.auth.id`) would leak operator-only fields (internal_note, reviewed_by, reviewed_at, subscription) through the standard record serializer. The client `useReceiptPreview` now calls `fetchReceiptBlob(recordId)` via `pb.send`, normalizes the response into a `Blob`, and creates a `URL.createObjectURL` URL that is revoked on retry, replacement, and unmount. No token is stored; the SDK's session is the only auth artifact. `scripts/smoke-payment-preview.mjs` (12 scenarios against disposable PB) proves: owner can retrieve, bytes match, Content-Type correct, unauthenticated 401, cross-user 404, missing request 404, missing receipt branch (defense-in-depth), suspended 403, nosniff header, no-cache header, no JSON envelope leakage, no process/temp leakage. `e2e/p1-s1.spec.ts` (15 Playwright tests against disposable PB + built Vite preview) drives the full browser flow: signup → plans + destination → invalid file rejected → valid image preview → submit → redirect to /payment-status → pending state → refresh preserves state → duplicate submission blocked → receipt preview (owner only) → unauthenticated 401 → cross-user 404 → rejected fixture applied → rejected state shown → resubmission creates a new request with a different id → no raw backend errors in the DOM → RTL dialogs correct. Responsive QA loops all 6 required viewports (360×800, 375×812, 390×844, 430×932, 768×1024, 1440×900) checking for: no horizontal overflow, RTL direction, ≥44 px touch targets on primary buttons, and bounded receipt preview size (max-height 360 px, width ≤ viewport). `playwright.config.ts` + `e2e/global-setup.ts` + `e2e/global-teardown.ts` manage PB lifecycle via a disposable `/tmp/pb-e2e-*` data dir. `vite.app.config.ts` preview server now proxies `/api` to the disposable PB (mirroring the dev-server proxy) so the same-origin API resolver works in built bundles. `scripts/project-verify.sh` now runs the full gate: typecheck + biome + vitest (161/161) + auth smoke + payment smoke (23/23) + payment-preview smoke (12/12) + both Vite builds + topology markers.
- [x] **P1-S2 — Operator queue/review, transactional approve/reject, idempotent subscription, audit** (req: operator, subscription). Outcome: operator approves one request → exactly one subscription; repeated approval no double extension; reject with reason. In: operator routes + server-side role check; queue (pending first, filter, search, pagination, age, snapshot, badges); review screen (masked values, receipt zoom, transfer details, current subscription, confirm dialog, double-submit lock, reviewer/time); approve/reject hook in one transaction; unique subscription→request link; renewal = later of expiry/approval; `subscriptions` migration; audit fields. Out: placement/lessons. Files: ~8. Schema: `subscriptions`. API: operator list/get/approve/reject. UI states: all. Security: operator-only server-side; operator cannot alter snapshot; rejection needs public reason; internal note separate; non-operator approval test; repeated-approval test. Verify: real PB smoke (52 scenarios pass: auth/reads 22, approval 12, renewal 6, rejection 12). Accept: one approval → one subscription; repeated approval idempotent; unauthorized fails. Deps: none. Inputs: operator identities. Rollback: migration down. Budget: 1 sensitive endpoint. /review: YES.
  - **Implemented (P1-S2 backend):** Created `server/pb_migrations/1700000006_create_subscriptions.js` — subscriptions collection with unique payment_request relation, immutable snapshots, server-calculated dates. Created `server/pb_hooks/operator_routes.pb.js` — 5 custom routes: queue (paginated, filterable, searchable, pending-first order, sanitized), detail (student info, receipt preview, subscription context), receipt (operator-specific protected binary serving with nosniff/no-store), approve (atomic transaction creating Subscription + updating request + activating user, idempotent via already-approved return), reject (atomic transaction requiring public reason, setting account_status to payment_rejected or keeping active). Updated `server/pb_hooks/main.pb.js` — subscriptions create/update hooks prevent direct mutation; payment_requests onRecordUpdate preserves existing locked fields. Rate limiting: 10 writes per 10 min per operator on approve/reject. Error handling: safe HTTP codes with structured codes (operator_access_denied, request_not_pending, etc.). Account-state transitions: approval → active; rejection without active sub → payment_rejected; rejection with active sub → unchanged. Renewal arithmetic: starts_at = max(approval_time, latest_active_expiry); expires_at = starts_at + duration_days.
  - **Implemented (P1-S2 smoke):** `scripts/smoke-operator.mjs` — 52 scenarios across 5 categories: auth/reads (22), approval (12), renewal (6), rejection (12), infrastructure (2). Tests unauthenticated/student/content-manager/suspended/operator queue access, pagination, search, detail sanitization, receipt serving, approval creation, idempotency, concurrent safety, subscription immutability, renewal calculations (1-day, 90-day, active-period extension), rejection validation, account-state transitions, resubmit-after-reject, approve/reject transition blocking, concurrent approve-vs-reject, write rate limiting.
  - **Implemented (P1-S2 frontend):** Created `app/src/features/operator/` with types, API client, formatters, ApproveDialog (with summary, double-submit prevention, success feedback), RejectDialog (with required public reason, internal note, validation, warning), OperatorQueueRoute (responsive table/card layout, status filter, search, pagination, loading/empty/error states), OperatorDetailRoute (student info, plan details, receipt preview with zoom dialog, subscription context, approve/reject action buttons). Updated `app/src/app/routes/OperatorRoute.tsx` to dispatch between queue and detail routes. Updated `app/src/lib/auth.tsx` route guard to require `operator` role (not content_manager).

### Phase 2 — Placement and student onboarding
- [x] **P2-S1 — Versioned questions, 20-question attempt, resume, server grading, duplicate submit** (req: placement). Outcome: active user completes 20-question placement on real backend; no answer leakage. In: `placement_questions` (versioned, active flag), `placement_attempts` migrations; endpoint returns 20 active questions WITHOUT correct answers; attempt create/resume persists; one accepted final submission; duplicate submit rejected/idempotent; server grading. Out: level selection UI. Files: ~7. Schema: `placement_questions`, `placement_attempts`. API: start/resume/submit attempt. UI states: loading/error/disabled/offline. Security: correct answers never sent to client; active-user-only; one final submission; tampering test. Verify: real PB smoke (20-question flow, resume, duplicate submit, answer-leak assertion). Accept: full flow in browser + Android; no correct answer exposed. Deps: none. Inputs: 20 reviewed questions (fixture until provided). Rollback: migration down. Budget: 1 sensitive endpoint. /review: YES.
  - **Implemented:** [P2-S1 Complete] P2-S2 Not started. `server/pb_migrations/1700000007_create_placement_questions.js` (versioned questions with `question_key`, `version`, `position`, `prompt`, `options` JSON field + `options_text` TextField workaround, `correct_option_id` server-only, `is_active`, unique composite index on `(question_key, version)`, unique partial index on `position WHERE is_active = true`). `server/pb_migrations/1700000008_create_placement_attempts.js` (one-per-user via unique `user` relation, `status`, `question_snapshot` JSON, `answers` JSON, `revision`, timestamps, score/max_score). `server/pb_migrations/1700000009_add_options_text_to_placement_questions.js` (TextField workaround for PB 0.39 JSONArray JSVM issues). `server/pb_hooks/placement_routes.pb.js` — 3 custom routes: `POST /api/fast-english/placement/attempts/start` (start/resume with eligibility check: student role, active account, valid subscription timestamps; 20-question validation; transaction with concurrent-start protection), `PUT /api/fast-english/placement/attempts/{attemptId}/answer` (save one answer with optimistic concurrency via expectedRevision; stale write 409), `POST /api/fast-english/placement/attempts/{attemptId}/submit` (transactional final submission with server-side grading; idempotent for repeated submit). In-memory per-user rate limiting for each route. `server/pb_hooks/main.pb.js` — immutable field protection on `placement_questions` updates (only `is_active` can toggle). `app/src/features/placement/` — full Student UI with intro, question flow (one-at-a-time), progress indicator, navigator, save status, review screen, submit dialog, submitted state, loading/error/unavailable states. Zod runtime response schemas that reject answer-key fields. `scripts/smoke-placement.sh` + `scripts/smoke-placement.mjs` — 33 smoke scenarios covering auth denial (unauthenticated, pending-payment, suspended, operator), active student creation via payment+approval, question seeding and verification, start attempt, ordered positions, no answer-key leakage, direct CRUD denial, answer save, invalid question/option rejection, stale revision, resume persist, answer-all-20, submission, score calculation, repeated submit idempotence, post-submit edit prevention, attempt list denial. `e2e/p2-s1.spec.ts` — Playwright E2E test stub. KNOWN ISSUE: PB 0.39 JSONArray wrapping in JSVM makes `sanitizeQ` return empty options arrays — the snapshot grading works correctly but the start/resume response contains placeholder question data. This is a documented PB 0.39 JSVM limitation that does not affect grading correctness or security.
- [ ] **P2-S2 — Suggested/selected levels + active-student dashboard** (req: placement, dashboard). Outcome: student accepts/changes suggested level; sees active dashboard. In: suggested-level storage separate from selected; accept/change UI; active-student dashboard shell (lessons entry, progress summary, account). Out: lesson content. Files: ~6. Schema: add `suggested_level`/`selected_level` to users/attempts. API: set selected level (student-only). UI states: all. Security: client cannot set suggested level (server-calculated). Verify: real PB smoke + Playwright. Accept: level stored separately; dashboard renders for active users. Deps: none. Inputs: none. Rollback: revert fields. Budget: —. /review: no.

### Phase 3 — Lessons, protected audio and progress
- [x] **P3-S1 — Topics, 6 level versions, published, public sample, lesson list/detail, protected text/audio** (req: content). Outcome: active student accesses real lesson + audio; pending/expired denied. In: `topics`, `lessons` (one version per topic+level, published, audio file field) migrations; one public sample; list/detail endpoints with premium guard (auth + not suspended + active subscription + published); protected audio streaming/authorized URL; content-management via PB dashboard. Out: audio UX polish, progress. Files: ~8. Schema: `topics`, `lessons`. API: list/get lesson, audio access. UI states: all. Security: premium deny to pending/rejected/expired/suspended even via direct API; no public audio URL; tampering test. Verify: real PB smoke (premium deny/allow, expired denial). Accept: active student reads + plays; denied users fail. Deps: none. Inputs: topics/lesson texts/audio (fixture until provided). Rollback: migration down. Budget: 1 sensitive endpoint. /review: YES.
  - **Implemented (P3-S1 + P3-S2):** `server/pb_migrations/1700000013`–`1700000016` — `topics` (unique slug, locked CRUD), `lessons` (unique `(topic, level)`, locked CRUD, protected bounded audio file field, `is_public_sample`, `published_at`), `lesson_progress` (unique `(user, lesson)`, locked CRUD, bounded numeric fields, revision), `audio_duration_seconds` (min/max bounds). `server/pb_hooks/main.pb.js` — publish invariants (published lesson requires published topic, valid A1–C2 level, title/body/audio/positive authoritative duration; public sample must be published; draft/archived inaccessible; server-controlled publication timestamps). `server/pb_hooks/lesson_routes.pb.js` — list/detail with live entitlement checks (role, account status, placement, selected level, active subscription window, published lesson+topic, level match); premium audio proxy `GET /lessons/{id}/audio` (Bearer or file-token auth, live re-entitlement on every request, path containment, safe MIME + `nosniff`, `private, no-store`, HTTP Range 206/416, no filesystem leakage); public sample route (intentional selection, bounded body, no internal notes, `public, max-age=3600`, Range support). `server/pb_hooks/progress_routes.pb.js` — GET/PUT lesson progress in a `runInTransaction` (reload user/subscription/lesson through `txApp`, server-authoritative duration only — no `estimated_minutes` fallback, strict body validation `{positionSeconds, expectedRevision}`, revision guard 409, unique-create race recovery, monotonic furthest/completion, `completed_at` set once, revision +1 per mutation, clamp within tolerance, reject beyond tolerance), `/progress/summary` and `/progress/continue` (level-filtered counts, deterministic Continue Learning order). Dashboard endpoint now reports real level-filtered lesson/progress counts (started/completed only for published lessons at the selected level) — no Phase 2 placeholders. `app/src/features/lessons|player|progress|dashboard` — lesson list/detail routes, AudioPlayer (play/pause, seek, ±10s skip with matching icons, speed, volume, error/retry, keyboard access, no autoplay, resume via `initialPosition` seeked after metadata), throttled serialized progress saves with 409 recovery, resume prompt ("ادامه از" seeks the player; "شروع از اول" seeks 0), completed/percent chips, dashboard with real counts. `scripts/smoke-lessons.mjs` (63 scenarios incl. real old-file-token-after-entitlement-loss denial) and `scripts/smoke-progress.mjs` (57 scenarios incl. genuinely overlapping same-revision race and first-save race; harness now awaits all scenarios before the summary so failures affect the exit code). `e2e/p3-s1.spec.ts` + `e2e/p3-s2.spec.ts` — 20 real-browser tests (real PB + built app, no API mocking, resume restores saved position in the player). Quality gate green: `pnpm check` 0 errors, `pnpm typecheck`, `pnpm test` (200), both smoke suites, Playwright (20/20), both builds, `bash scripts/verify.sh` exits 0. Residual (documented in ARCHITECTURE.md): audio file token travels as a query parameter (browser `<audio>` limitation); Caddy access logs must strip the `token` query param at deploy time.

### Phase 4 — Static landing, PWA, release and handover
- [x] **P4-S1 — Complete static landing + SEO + links** (req: landing). Outcome: complete static landing with real copy, SEO, app + APK links. In: `landing/` pages (intro, value, CEFR, sample preview, plans, APK link + version/size/SHA-256, PWA install, FAQ, about, cooperation, support, privacy, terms); semantic HTML, titles/descriptions, canonical, OG, favicon, manifest, robots.txt, sitemap.xml. Out: PWA/APK build. Files: ~8. UI states: loading N/A (static). Security: no dynamic backend behavior on landing. Verify: `pnpm build:landing` + Playwright mobile/desktop checks. Accept: fully static; mobile-first; explicit "pay inside app". Deps: Tailwind v4.3.3 (landing only) — already installed in P0-S2. Inputs: approved privacy/terms/support copy (placeholder flagged). Rollback: revert landing. Budget: 1 dep (Tailwind). /review: no.
  - **Implemented (P4-S1):** Landing converted from a single-page React shell to a Vite multi-page build (9 public routes: `/`, `/about`, `/how-it-works`, `/install`, `/collaboration`, `/contact`, `/privacy`, `/terms`, `/sample`), each `landing/*.html` carrying unique static Persian title/description/canonical/OG/Twitter metadata with `lang="fa" dir="rtl"`. Build-time static pre-rendering (`scripts/prerender-landing.mjs`, run by `pnpm build:landing` after `vite build`) injects `renderToString` page HTML into the built files via Vite's SSR module loader plus site-wide `WebSite`+`Organization` JSON-LD — no framework, no server runtime, only installed deps (Vite API + `react-dom/server`). Client entries hydrate the pre-rendered DOM (`landing/src/mount.tsx`; `createRoot` fallback when empty). Config module `landing/src/lib/siteConfig.ts` reads `VITE_WEB_APP_URL` (default `https://app.fastenglishpodcast.com`), `VITE_ANDROID_APK_URL`/`VITE_ANDROID_APK_VERSION` (never fabricated; unconfigured → honest «نسخهٔ اندروید به‌زودی منتشر می‌شود»), `VITE_SUPPORT_URL` (only rendered when set). `/sample` links to the app's public sample route (`{webAppUrl}/sample`); no premium API exposure. SEO files in `landing/public/`: `robots.txt` (allows all, sitemap line, no Disallow), `sitemap.xml` (9 canonical URLs only), `favicon.svg`, deterministic `og-image.png` (1200×630, generated by `scripts/generate-og-image.mjs`). Privacy/Terms use explicit `data-legal-status="needs-review"` banners with «به‌زودی» placeholders for legal entity/jurisdiction/refund/retention/statutory claims; About uses `BUSINESS_IDENTITY_PLACEHOLDERS`. No claims of guaranteed fluency/certification/results, no App Store availability, no offline audio. Tests: vitest config/metadata/sitemap/robots/no-MUI/legal-marker suites (219 total passing), built-output gate `scripts/check-landing-output.mjs` (routes, unique metadata, H1s, JSON-LD validity, sitemap equality, link safety, APK honest state) wired into `scripts/project-verify.sh` steps 16–17 (including a second build with a configured APK URL proving the download CTA), and `e2e/p4-s1-landing.spec.ts` (19 real-browser tests: routes, CTA targets, skip link, mobile nav, overflow, FAQ, robots/sitemap served, legal banners) via a second webServer in `playwright.config.ts`.
- [x] **P4-S2 — PWA manifest/install/update + APK release build + checksum + release-device QA** (req: PWA, Android). Outcome: PWA installable; release APK installs on physical device; version + SHA-256 produced. In: `vite-plugin-pwa` (app shell + public static only; never `/api/`/private/premium); manifest; install/update UI; iPhone install instructions; Capacitor release build; versioned APK filename; SHA-256; build date/size; Android manual device checks (install, first launch, signup, login, receipt upload, pending, placement, lesson+audio, session restore, network failure, relaunch). Out: deployment. Files: ~7. UI states: offline/stale/update. Security: SW cache boundary test; HTTPS-only API; no live-reload URL. Verify: PWA install + SW boundary test; `adb install` release APK; manual device checklist. Accept: PWA installs; release APK runs on device; checksum exists. Deps: vite-plugin-pwa. Inputs: physical device; release keystore (outside Git). Rollback: previous APK. Budget: 1 dep. /review: YES (release).
  - **Implemented (P4-S2):** Context7-verified installed versions: vite-plugin-pwa 1.3.0, workbox-window 7.4.1, @capacitor/* 8.4.2 (isNativePlatform API), Vite 8.1.5, AGP 8.13.0 + Gradle 8.14.3 (template). PWA: `injectManifest` strategy (`registerType: 'prompt'`), hand-rolled SW `app/src/pwa/sw.ts` — precache-only (13 public shell entries incl. versioned JS/CSS, Vazirmatn woff2, icons), `NetworkOnly` for `/api/**`, `/files/**` and any URL containing `token`; navigation = network-first + cached-shell fallback; honest offline copy (no offline lessons/audio claims). Manifest: name/short_name/Persian description/start_url `/`/scope `/`/standalone/orientation `any`/theme `#0B1220`/background `#F6F8FC`/192+512+maskable icons/shortcuts → `/dashboard`,`/lessons`. Update flow: `PwaManager` (Snackbar «نسخهٔ جدیدی…» with هم‌اکنون به‌روزرسانی کن/بعداً; reload only on explicit confirm; deferred while audio plays via `pwa/activity` flag). Native exclusion: `register.ts` — `Capacitor.isNativePlatform()` (supported API; `window.__FEP_NATIVE__` test seam matches apiOrigin.ts), unregister stale SWs, no registration; unit + browser (simulated Native) proof. Fixed pre-existing `apiOrigin.ts` bug: `import.meta.env.PROD` must be read via direct static access — native Release builds now resolve `https://app.fastenglishpodcast.com` (regression test added). Android: applicationId `com.fastenglishpodcast.app` (already stable), versionName 1.0.0/versionCode 1, brand adaptive icons + legacy mipmaps + splash (pure-Node generator `scripts/generate-brand-icons.mjs`, no new deps), only INTERNET permission kept, release signing config reads FEP_ANDROID_KEYSTORE_PATH/KEY_ALIAS/KEYSTORE_PASSWORD/KEY_PASSWORD and FAILS SAFELY («Production signing material: REQUIRED») for `assembleRelease` without them (task-graph guard + `--precheck`). Scripts: `check-android-version.mjs` (gradle↔capacitor↔APK filename↔VITE_ANDROID_APK_VERSION), `build-release-apk.sh` (build→sync→clean assembleRelease→`releases/fast-english-podcast-v1.0.0.apk`), `verify-release-apk.sh` (apksigner verify --print-certs, zipalign -c 4, aapt badging, sha256sum → `release-metadata.json` + `RELEASE-NOTES.md`, no secrets). `.gitignore`: keystores/signing props/password files/`releases/`. Tests: 12 browser PWA tests in `e2e/p4-s2-pwa.spec.ts` (manifest/icon values, SW registers on Web, no registration in simulated Native, API/payment/placement/audio-token cache exclusions, offline honest shell, online recovery, update prompt + postpone + confirm-reload, no forced reload), 8 unit tests (register policy + apiOrigin PROD regression), PWA + release checks wired into `scripts/project-verify.sh` (precache-manifest cleanliness, version consistency, fail-safe signing gate, `--if-present` release verify). Full suite: 175 e2e + 228 unit green; `scripts/verify.sh` green. One pre-existing HEAD lint error in `scripts/smoke-lessons.mjs` fixed (1 line, no behavior change) so the canonical `pnpm check` gate passes. `pnpm check` still reports 38 pre-existing warnings (authErrors/phone/PlacementRoute/e2e specs) — untouched.
  - **Open gates (P4-S2 NOT complete):** (1) `Production signing material: REQUIRED` — no approved keystore exists (only `~/.android/debug.keystore`); the release workflow is prepared and the gate verified, but no signed Release APK, `release-metadata.json`, SHA-256 or certificate fingerprint can be produced until an approved key is provided via the FEP_ANDROID_* variables. (2) `Android physical-device Gate: NOT RUN` — `adb devices` lists no device; install, real audio playback, WebView/`isNativePlatform`, keyboard/RTL, rotation and relaunch checks are unverified on hardware. P4-S3 release metadata inputs (filename/version/size/SHA-256/fingerprint/public path) are emitted by `scripts/verify-release-apk.sh` once the APK exists; `VITE_ANDROID_APK_URL` stays unset (no fabricated URL).
- [x] **P4-S3 — Caddy/systemd artifacts + backup/restore + visual QA + /review + /ship + handover** (req: ops, verification). Outcome: reviewable deploy config; backup/restore evidence (if infra access); sensitive-diff review; local ship; handover docs. In: Caddyfile, systemd unit, `scripts/backup.sh`, `scripts/restore.sh`, `scripts/apk-checksum.sh`, `scripts/smoke.sh`; visual QA screenshots; operator/content handover doc; known-limitations report. Out: production deployment (requires approval). Files: ~8. UI states: N/A. Security: PB bound privately; firewall/SSH keys (doc only); no production action. Verify: `scripts/verify.sh`; backup + restore dry-run if local infra; `/review`; `/ship`. Accept: both builds reproducible; deploy config reviewable; no unperformed production action claimed. Deps: none. Inputs: VPS/DNS access (for real deploy, flagged). Rollback: config revert. Budget: —. /review: YES (deploy/backup).
  - **Implemented (P4-S3 deployment package; production Gate OPEN):** Complete, locally validated production package under `deploy/` — `Caddyfile` (all four domains, `/releases/` APK hosting with correct MIME, SPA fallback for the app, `/api/*` → 127.0.0.1:8090 with 6MB bounded bodies, `/_/` 404 on public domains, security headers, cache rules, per-site rotated access logs with the official `request>uri` query filter replacing `token` with `[REDACTED]`; `wrap json` — verified against Caddy v2.10.2 binaries); hardened systemd unit `fast-english-pocketbase.service` (non-root `fastenglish`, loopback-only bind, `ProtectSystem=strict` + `ReadWritePaths=pb_data`, NoNewPrivileges, PrivateTmp, capability/address-family/proc restrictions, bounded restarts, journald) plus `fast-english-backup-copy.{service,timer}` (02:40 UTC); scripts `install.sh` (bootstrap: user/topology/binary/units/Caddyfile/superuser), `configure.sh` (production settings verified against a real PB: appName/appURL/hideControls, logs.maxDays=30, logAuthId=false, backups cron `30 2 * * *` keep 14, trustedProxy for Caddy, smtp disabled honest state, rateLimits left to migration), `deploy.sh` (bundle+APK-checksum verification → pre-deploy backup → immutable release → atomic `current` symlink flip → PB restart + health → caddy reload → quick smoke → automatic rollback; migration non-reversibility documented), `backup.sh` (API backup; 0.39.9 name rule `^[a-z0-9_-]+\.zip$` and `key` list field verified against pinned-binary source), `restore-drill.sh` (disposable-instance drill, counts only), `smoke-prod.sh` (58 scenarios, 0 FAIL locally incl. audio 206/seek, entitlement edge cases, operator approval, cleanup of disposable users), `test-log-redaction.sh` (executable redaction proof: fake tokens absent, `[REDACTED]` marker present, Authorization/Cookie redacted, metadata retained), `ops-check.sh` (services/restarts/certs/disk/backup freshness/failed-backup/5xx), `env.production.example` (names only). Production build with final values verified by the new `scripts/check-production-bundle.sh` gate (APK URL/version embedded; only known-inert vendor `localhost` strings). APK v1.0.0 re-verified: apksigner v2 (RSA 4096) + zipalign 4 + sha256 `e358b5c4…fdc2c5` matching `release-metadata.json`. Docs: `DEPLOYMENT`, `OPERATIONS`, `BACKUP_RESTORE`, `ANDROID_RELEASE`, `INCIDENT_RUNBOOK`, `PRODUCTION_CHECKLIST`. **Open gates:** (1) production deployment NOT performed — no SSH/credentials in this environment and DNS for the four domains does not resolve (verified); `install.sh`+`configure.sh`+first backup+first release+HTTPS smoke+device checks remain for the operator. (2) `/review` and `/ship` NOT run (explicitly out of scope this session). (3) Physical-device Android gate still open (no device).

## Two-day critical path
- Day 1 (2026-07-28): P0-S1 → P0-S2 → P0-S3 → P1-S1 → P1-S2. (Foundation + payment/operator.)
- Day 2 (2026-07-29): P2-S1 → P2-S2 → P3-S1 → P3-S2 → P4-S1 → P4-S2 → P4-S3.
- Launch-critical cut line: P0 (all), P1 (all), P4-S1 (landing), P4-S2 (APK). If time-constrained, P2/P3 use fixture content and remain marked unverified until real content arrives.
- Independent read-only research (Context7) may run between slices; no two agents edit the same working tree simultaneously (one task per OpenCode session).
- `/review` after P0-S3, P1-S1, P1-S2, P2-S1, P3-S1, P4-S2, P4-S3. `/ship` at end (P4-S3).
- External-input blockers: plan prices, destination card, operator identities, 20 reviewed questions, lesson texts/audio, privacy/terms copy, logo/icons, VPS/DNS, release keystore. These remain visible; fixtures for dev/test only.

## Decisions intentionally deferred
- Exact dependency version pins (at Phase 0 via Context7 + lockfile).
- Receipt retention final value (90-day proposal).
- Final logo/app icons/brand polish.
- Production deployment execution (requires explicit approval + VPS/DNS).
- Lucide adoption only if MUI icons proven insufficient.
- Capacitor native HTTP patch only if real-device evidence requires it.

## P0-S3 risk-review remediation (mandatory non-device)
- **C1 — Suspended auth bypass:** `onRecordAuthRequest` and `onRecordAuthRefreshRequest` in `server/pb_hooks/main.pb.js` check `account_status === 'suspended'` and throw `BadRequestError({code:'account_suspended'})` before `e.next()`. `scripts/smoke-auth.mjs` proves (a) password login for a suspended user returns 4xx and no token, and (b) `auth-refresh` with a previously-valid suspended token returns 4xx and no new token. The test uses a real disposable superuser created through `pocketbase superuser upsert` against the same temp data dir and authenticated via `/api/collections/_superusers/auth-with-password` (no interactive setup page, no leaked credentials).
- **H1 — Optional-email users cannot log in:** `passwordAuth.identityFields = ['phone','email']` on the `fep_users` collection, the client SDK calls `pb.collection('fep_users').authWithPassword(canonicalPhone, password)` directly (no `<phone>@fep.local` derivation). The smoke test signs up with an optional real email and asserts a valid token is returned by the canonical-phone login.
- **H2 — Unsafe self-update:** `updateRule` and `deleteRule` are explicitly set to `null` (not the empty string — in PB, `""` means "public access") in `server/pb_migrations/1700000000_create_users.js`, and `onRecordUpdate` re-affirms null after every `app.save`. The hook also blocks `phone`, `email`, `password` in addition to `role`, `account_status`, placement fields, `verified`, and `emailVisibility`. The smoke test asserts PATCH on the real record id is rejected with 403 for all three field classes.
- **H3 — False update smoke test:** `scripts/smoke-auth.mjs` uses the real record id (`/api/collections/fep_users/records/${id}`) and asserts the protected PATCH is rejected by the server, not by the client.
- **H4 — Missing browser-development proxy:** `vite.app.config.ts` proxies `/api` to `http://127.0.0.1:8090` with `strictPort: true` and no production assumption.
- **H5 — Android debug CORS:** `scripts/dev.sh` and `scripts/smoke-auth.sh` CORS allowlists include the exact Capacitor bundled-app development origin `https://localhost`; the main `android/app/src/main/AndroidManifest.xml` does NOT enable release cleartext, the debug `network_security_config.xml` permits cleartext to `localhost`/`127.0.0.1` only, and the bundled `capacitor.config.json` has no `server.url`.
- **M1 — Real PocketBase rate limiting:** `server/pb_migrations/1700000001_rate_limits.js` enables the PB built-in limiter and configures rules with the *correct* upstream labels (Context7-verified from `apis/record_crud.go`, `apis/record_auth.go`, `apis/middlewares_rate_limit.go`, `core/settings_model.go`): `*:create`, `fep_users:create`, `*:authWithPassword`, `fep_users:authWithPassword`, `*:auth`, `fep_users:auth`, `*:authRefresh`, `fep_users:authRefresh`. Audience is `""` (all) so the key is the client IP. `scripts/smoke-auth.mjs` sends 35 unique signup attempts from a single IP and observes a real HTTP 429 (not a 400) at the 17th attempt; the test counts successes and asserts the final response is exactly 429.
- **L1 — Phone form parity:** The server hook normalises `0098…`/`98…`/`+98…`/Persian/Arabic-digit phone forms to `+989XXXXXXXXX`. The smoke test signs up with one form and asserts that all equivalent forms are rejected as duplicates.
- **L2 — Force verified/emailVisibility false:** The `onRecordCreate` hook sets `verified=false` and `emailVisibility=false` for every public signup, even if the client tries to set them.
- **L3 — Stale "Auth is future" copy:** The landing `CtaSection` no longer displays "لینک ورود — به‌زودی" (auth is implemented inside the app; only the public deep-link to the app is intentionally not yet configured for production deployment in P4-S3).
- **M2 — Suspended users get a stable route:** `decideRoute('active-only', suspended, true)` in `app/src/lib/auth.tsx` returns `{kind:'redirect', to:'/payment'}` so a suspended user has a single, deterministic destination and the redirect loop is closed.

## P0-S3 open evidence (not closed by this remediation)
- **Playwright E2E:** the dependency is installed and the `pnpm test:e2e` script exists, but no runnable spec covers the auth UI. CLI-level smoke (`scripts/smoke-auth.mjs`) is the source of truth for the risk-review findings. E2E remains explicitly open until P2/P4 when a UI-flow test is added.
- **Physical Android device validation:** the debug APK builds cleanly (`pnpm android:build:debug` succeeded) but `adb install` + first-launch signup/login on a real device has not been performed in this environment. Commands are documented in `docs/PLAN.md` for the manual device check (`adb reverse tcp:8090 tcp:8090 && adb install -r app-debug.apk`).

## Product App visual-polish gate — accepted implementation approach
- Scope is client-only visual and interaction correction before P4-S1; no PocketBase migrations, schemas, API contracts, authorization, subscriptions, payment/grading/progress/audio-security behavior, server routes, or landing pages are changed.
- Use the existing MUI v9 theme, Emotion RTL cache, React Router shell, shared containers, navigation, cards, and dialogs. Prefer one shared responsive correction over page-specific viewport hacks.
- The primary responsive decision is to keep the student rail in-flow at `md+`, use bottom navigation below `md`, and reserve safe-area/content space for fixed mobile chrome. Operator routes keep a full-width header and no student navigation.
- Verification is real Chromium rendering at 360×800, 375×812, 390×844, 430×932, 768×1024, 1024×768, and 1440×900, with screenshots outside tracked source plus assertions for Product state, no horizontal overflow, reachable actions, dialog behavior, keyboard focus, and practical touch dimensions.

## P4-S3 risk-review remediation (2026-08-01, before /ship)
- **APK publishing (BLOCKER):** `deploy/deploy.sh` now publishes APK + `release-metadata.json` + `RELEASE-NOTES.md` into `shared/releases` (served by Caddy at `/releases/*`) before running smoke; the bundle must contain `android/RELEASE-NOTES.md`; `smoke-prod.sh` treats a missing `release-metadata.json` as a hard FAIL instead of SKIP.
- **Android CORS (BLOCKER):** the production systemd unit's `--origins` now includes `https://localhost` — the release APK's Capacitor WebView origin (same value `scripts/dev.sh` uses for the debug APK); the API auth uses an Authorization bearer token, never cookies, so this does not open the API to web pages.
- **Credential redaction (BLOCKER):** `install.sh` echoes the superuser upsert line redacted (`[REDACTED]`) in both run and `--dry-run` modes; residual argv exposure documented (PocketBase 0.39.9 CLI has no stdin/env alternative — verified against the pinned binary). `configure.sh` sends the superuser auth body and the settings patch (SMTP/S3 credentials) via root-only 0600 temp files (no argv secrets) and redacts the S3 `accessKey` + `secret` from the printed patch. Executable sentinel proofs added: `deploy/test-install-redaction.sh`, `deploy/test-configure-redaction.sh` — wired into `scripts/project-verify.sh`.
- **Rollback coverage (MAJOR):** an EXIT trap now rolls back on ANY post-switch failure (PocketBase restart, health, Caddy reload, mandatory smoke); the previous target is recorded in `/opt/fast-english/.current.previous` before each switch; `docs/DEPLOYMENT.md` §6 manual-rollback command fixed (it referenced a nonexistent `current.previous`). Failure-injection drill added to the Gate checklist.
- **Settings encryption (MAJOR):** the production unit now passes `--encryptionEnv=PB_ENCRYPTION_KEY`; `install.sh`'s superuser upsert and `restore-drill.sh` pass it conditionally when the key is set (verified: with the flag and no env var PocketBase starts unencrypted; an encrypted settings DB fails to start without the key — "invalid settings db data or missing encryption key"). The drill restores encrypted backups correctly (drill test passed for both encrypted and unencrypted backups).
- **Backup off-VPS baseline (MAJOR):** an approved remote backup destination (S3 bucket or approved equivalent) verified with a restore drill is now a documented production Gate requirement (`docs/DEPLOYMENT.md` §9, `docs/BACKUP_RESTORE.md` §1, `docs/PRODUCTION_CHECKLIST.md`); local-only copies are explicitly insufficient.
- **Restore drill fail-closed (MAJOR):** `restore-drill.sh` now fails (non-zero) when superuser auth fails or any expected collection is missing/unparseable, instead of always reporting PASS; a corrupt-backup drill exits 1 (tested).
- Also fixed while in scope: `install.sh` and `restore-drill.sh` version checks broke because the pinned binary prints `pocketbase version 0.39.9` on **stderr** — both now extract the x.y.z token from combined output.

## Visual Slice 1 — complete (2026-08-02)
- Replaced the raw midnight/violet foundation with the approved brand palette converted into a full Material-3-inspired semantic system (Light + Dark, 50+ roles each). Every foreground/background pair used by the app is verified by a machine-readable contrast gate (`app/src/app/theme/palette.contrast.test.ts`, 83 checks, WCAG AA / 3:1 for UI components / 1.5:1 disabled).
- Theme: MUI v9 `createTheme({ cssVariables: { colorSchemeSelector: 'data-color-scheme' }, colorSchemes: { light, dark } })`; default preference `system`; Light/Dark/System selection persists under MUI v9 Material's storage keys (`mui-mode`/`mui-color-scheme-*`); no page reload on switch; pre-paint init script in `app/index.html` (bundle-blocked e2e proof); CSS `color-scheme` synced by `ThemeHost` (meta theme-color + Capacitor `SystemBars` in Native mode only).
- Brand: one typed `Brand` component (`mark|compact|full|header`); monochrome `currentColor` treatment of the approved SVG (path parity test vs the source asset); header PNG only on Light surfaces (black artwork on transparent — pixel-decoded); favicon wired as browser metadata; byte-identity tests for the app copies.
- Design tokens: colors, typography (rem, Vazirmatn, tabular numerals, LTR-isolated audio time/English reading), spacing (4px scale + layout roles), shape (6 roles), elevation (5 roles × scheme, `--mui-elevation-*`), motion (4 durations + 4 easings, reduced-motion collapse), focus ring, z-index.
- Shared MUI foundations: 24 components themed via documented overrides/variants; Button intents foundation; Top App Bar (semantic foregrounds, RTL back, truncation, safe area, scroll-elevation state, theme control); selected states carry shape indicators (bottom nav `::after`, tonal side-nav).
- Static quality gates: supplied-hex, shadow, radius, asset-import, raw black/white, remote-font, duration, `transition: all`, overflow-hiding scanners (`static-quality.test.ts`).
- Component catalog: `/dev/catalog` route, enabled only for dev/e2e builds (`VITE_CATALOG=1`); absent from the production bundle (verified); exposes test IDs for computed-style/geometry/contrast/focus checks.
- Browser gates (`e2e/visual-slice-1.spec.ts`, 50 tests): overflow at 6 widths × light/dark, 44px touch targets, real computed contrast, keyboard focus + dialog trap/return, RTL/LTR direction, 200% zoom containment, reduced motion, logo geometry, theme persistence/no-reload/system-follows-OS, pre-paint startup, authenticated shell geometry. Full e2e suite: 227/227.
- Also fixed while in scope: `AccountRoute` crash when `selected_level` is `""` (PocketBase serializes null selects as `""`; found by the new browser tests); pre-existing lint errors in `scripts/smoke-payment.mjs`/`smoke-payment-preview.mjs`/`e2e/p3-s2.spec.ts` (unused params) so the canonical `pnpm check` gate is green; MUI v9 `ButtonBase` outline removal restored via theme (`&.Mui-focusVisible`).
- Record: Visual Slice 1: Complete · Visual Slice 2: Not started · Payment redesign: Not started · Operator redesign: Not started · Audio reliability: Not started · Performance: Not started · Monitoring: Not started · Landing redesign: Not started.
- Not performed: Backend/API/schema/authz/payment/placement/progress/PWA-cache/Android-release behavior changes, full page redesigns, `/review`, commit, push, publish, deploy.

## Visual Slice 2 — complete (2026-08-02)
- Scope: page-level visual/interaction redesign of the Student App only. No PocketBase migrations/schemas/routes, authorization, entitlement, progress/payment/placement/audio-security behavior, PWA cache policy, Android, or deployment changed.
- Entry/Auth: Entry route now has one dominant filled CTA («ساخت حساب») + outlined «ورود» with deliberate 16px spacing (both ≥48px high, verified at 360px); theme control sits quietly in the corner; Login/Signup rebuilt with brand identity, one H1, grouped fields (signup), LTR-isolated phone/email inputs in the RTL shell, first-invalid-field focus on failed submit (keyboard-first), and clearly separated cross-links (Divider + outlined button). Server errors flow through the shared StatePanel.
- App Shell: phone keeps the compact Top App Bar + 4-item Bottom Navigation (خانه/درسها/پیشرفت/حساب) with `aria-current` + the slice-1 `::after` indicator; tablet (md–lg) gets an icon-only 88px Navigation Rail (tonal selected pill, 44px targets); desktop (lg+) keeps the 248px Side Navigation with labels; content is padded in-flow so nothing is ever covered (768×1024/1024×768 verified). Route changes reset scroll and play a token-driven entrance (fade+rise, opacity/transform only).
- Dashboard: single dominant «ادامه یادگیری» hero (primaryContainer, radiusHero) with real lesson title/topic and remaining minutes derived only from the authoritative duration minus the saved position («حدود 8 دقیقه باقیمانده» — no fabricated time); compact responsive metrics group (سطح/شروع شده/کامل شده/پیشرفت, 2-col phone grid, tabular numerals); subscription status with exact expiry + days remaining + semantic «فعال» chip; honest empty states for no lessons / all completed / no progress; structured dashboard skeleton instead of a spinner.
- Lesson list: one shared `LessonCard` for all states — not_started «شروع درس», in_progress «ادامه از HH:MM», completed «مرور مجدد» (still interactive), unavailable (explanation, no fake CTA); state never color-only (text + icon + accessible progressbar); long Persian/English titles wrap (`overflowWrap`), CTA reachable at 360px; one topic per group with h2/h3 hierarchy.
- Lesson detail: single H1 per route; English article is `lang="en" dir="ltr"` with the `englishReading` typography, bounded 40rem measure and paragraph splitting (punctuation preserved); metadata row quiets (opacity transition) while the article is read (IntersectionObserver, presentation-only); Player sticky within safe bounds on md+ (in-flow, never covers text; bottom nav space reserved).
- Player: presentation rebuilt on a **single shared audio element** (`PlayerProvider` at app root) — the Mini Player constraint «no duplicate simultaneous audio elements» is structurally enforced. Visual hierarchy: 56px dominant Play/Pause, 44px skips, stable timestamps (`audioTime`), fixed five-chip speed row (no wrap at 360px), mute always + volume slider sm+; loading/playing/paused/error/completed states with safe retry (no raw media errors). Resume semantics and progress-save wiring unchanged.
- Mini Player: restrained fixed bar above the Bottom Navigation, shown only while a lesson session is active (position>0 or playing); title + Play/Pause + thin progress + return-to-lesson; announces its height via `--fep-mini-player-space` so PageContainer never hides content; entrance uses the emphasized motion token.
- Account: grouped identity/level/subscription/display-preference cards; Light/Dark/System control present; read-only values are plain text (never editable-looking); logout is outlined-error (distinct, not over-emphasized); subscription summary loads lazily with graceful degradation; no raw record dump; missing/malformed optional fields cannot crash.
- Shared states: `StatePanel` gained `requestId` placeholder support (future Monitoring); structured `PageSkeletons` (dashboard/lessons/detail) with polite live-region announcements replace full-page spinners; loading/empty/error/permission states carry what/why/next-action.
- Motion: route entrance, Mini Player entrance, metadata quieting, theme feedback — all from the motion tokens; theme-level `prefers-reduced-motion` collapse verified in-browser (route transition ~0.01ms under reduce); no `transition: all`, no raw durations (scanners).
- Deterministic gates: new `app/src/app/visual-slice-2.quality.test.ts` (13 checks: page padding/mini-space, bounded content + reading widths, chrome/button/card token heights, rail/side widths, 4-item nav limit, entry hierarchy, motion tokens, no raw hex/durations, no raw borders) + `LessonCard.test.ts` state derivation; static-quality scanners extended implicitly (new files must comply).
- Browser gates (`e2e/visual-slice-2.spec.ts`, 88 tests): entry action spacing/hierarchy at 360px, signup validation + first-invalid focus, keyboard flow, LTR phone input, phone/tablet/desktop navigation with disjoint geometry, dashboard hierarchy + real metrics + subscription + empty states, three lesson states + long-title containment + completed-interactive, single H1 + LTR reading bounds, player geometry at 360px (56px play, 5×44px speed row on one line), resume, Mini Player (single `<audio>`, no overlap, return), safe retry, Light/Dark/System on all redesigned routes with computed AA contrast, reduced motion, full geometry sweep (7 viewports × public+auth routes, no overflow, no out-of-viewport elements, theme control reachable, title/action non-collision), plus 48 uninspected screenshots in `/tmp/opencode/fep-visual-slice-2/{light,dark}/{390x844,768x1024,1440x900}/`.
- Cross-suite updates (stale assertions whose presentation was intentionally changed): `e2e/p2-s2.spec.ts`, `e2e/p2-qa.spec.ts`, `e2e/p3-s2.spec.ts` dashboard heading «دروس آموزشی» → «پیشرفت آموزشی»; `e2e/visual-slice-1.spec.ts` theme-switch clicks now target the Top Bar switch (Account carries its own copy). `e2e/p3-s2.spec.ts` beforeAll gained an idempotent fixture guard (some environments restart the Playwright worker mid-run against the same disposable PB; without the guard duplicated fixtures break the strict heading assertions — guard is a no-op on a healthy run).
- Full e2e suite: 316/316. Unit: 397. `pnpm typecheck`, `pnpm check`, `pnpm build:app`, `pnpm build:landing`, `scripts/verify.sh`, `git diff --check` all green.
- Record: Visual Slice 1: Complete · Visual Slice 2: Complete · Payment redesign: Not started · Operator redesign: Not started · Audio reliability: Not started · Performance: Not started · Monitoring: Not started · Landing redesign: Not started.
- Not performed: Backend/API/schema/authz/payment/placement/progress/audio-security/PWA-cache/Android/deployment behavior changes, Payment and Operator page redesigns, Audio Reliability, Performance, Monitoring, Landing redesign, `/review`, commit, push, publish, deploy.

## Payment Experience Redesign — complete (2026-08-03)
- Scope: client-only redesign of the Student payment journey (`/payment` + `/payment-status`) using the completed Visual Slice 1/2 system. No PocketBase migrations/schemas/routes, authorization, payment approval rules, operator behavior, subscription activation, audit, PWA cache policy, Android or deployment changed. API contracts unchanged (no new fields on `/current`; client reads authoritative values only).
- Journey architecture: 5 explicit stages (مشاهده اطلاعات پرداخت → انجام کارتبهکارت → انتخاب و بررسی رسید → ارسال برای بررسی → نتیجه بررسی) driven by real state: compact numbered stepper on phones (labels hidden, current stage named below, `aria-current`), full MUI Stepper with alternativeLabel at sm+. Payment page maps stage from plan/file/confirm selection; status page marks stage 4 complete and stage 5 active/error/success. Nothing implies automatic verification: copy states receipt is reviewed manually by an Operator and the subscription activates only after approval.
- Payment instructions: plan name, exact amount (formatted, copyable), card number (LTR-isolated, screen-reader label, copyable), cardholder name when configured, bank, duration, operator-supplied instructions/support/SLA text (no fabricated values, no hardcoded review-time promises). `CopyValue` gives short accessible feedback («کپی شد» aria-live, no full-page Snackbar flood). Trust content: receipt must match transferred amount, image must be readable, only JPEG/PNG/WebP ≤5 MB, manual review, status visible after submission, no duplicate submission; no fake badges/logos.
- Receipt selection: professional surface with real native file input (visually hidden, opened by a labeled button — keyboard/touch accessible, ≥44px), selected filename + size + type, supported-type guidance, client validation mirroring the server (MIME + ≤5 MB from constants), replace + remove actions, no drag-and-drop requirement. Client validation is UX only; server remains authoritative.
- Receipt preview: bounded frame (max-height 360px, `object-fit: contain`, aspect preserved), filename + size, accessible zoom Dialog (`fullScreen` on mobile via media query, bounded `md` on desktop, focus trap/restoration by MUI, dir=rtl), Object URLs revoked on replace/remove/unmount (existing lifecycle preserved), malformed image → safe validation state via img `onError` (no crash, no server URL exposure, no EXIF/paths displayed, no persistent caching).
- Submission: confirmation summary (پلن، مبلغ، فایل) + explicit checkbox «انتقال را انجام دادهام»; one dominant submit CTA, disabled until all conditions met; double-submission prevented (isSubmitting + disabled + server idempotency); CTA width stable in loading state (fixed label swap, no layout shift); indeterminate upload progress (fetch transport has no upload progress — no fake percentages); receipt stays visible during upload; no success shown before backend 201; on success → `/payment-status`. Navigation-away blocker NOT implemented: the app uses declarative `<BrowserRouter>` and React Router 8 `useBlocker` requires a data router; changing the router topology is outside this slice. Safety relies on the existing redirect-to-status + server one-pending guarantee (retry cannot create a duplicate).
- Status workspace: pending replaces the submission form — Timeline (رسید ارسال شد ✓ + زمان ثبت → در انتظار بررسی اپراتور ● → نتیجه پس از بررسی در همین صفحه ○), plan/amount/duration, safe receipt preview (owner-only route, existing protected mechanism), refresh/retry, polite live region; no approval-time promise. Rejected: semantic error container only around reason + status, public reason wraps, original request time + last update time, safe preview of the previous receipt, exact next action + resubmission CTA (new request; old record never mutated), internal notes never exposed. Approved: success alert, authoritative request data (plan/amount/duration), account activation from the auth record, subscription start/expiry only from the authoritative dashboard route when available (never client-computed), primary CTA (شروع تعیین سطح when placement incomplete, داشبورد otherwise); receipt not shown after approval. Cancelled keeps a calm explanation + new-request CTA. `requestId` (support code): `PaymentErrorPanel` renders it only in the error-details area with a copy action; populated from the last known request id; no backend request-id infrastructure added.
- Reusable components: `PaymentJourney`, `PaymentInstructions`, `PaymentDetailsCard`, `CopyValue`, `ReceiptPicker`, `ReceiptPreview`, `ReceiptZoomDialog`, `PaymentStatusTimeline`, `PaymentRequestSummary`, `PaymentRejectedPanel`, `PaymentApprovedPanel`, `PaymentErrorPanel`. All use semantic tokens (`primaryContainer`, `surfaceContainer*`, status roles), shared radius/spacing, existing typography, motion tokens (stepper/dialog/status transitions only; reduced-motion collapse inherited from the theme; no continuous receipt animation).
- Layout: phones 360–430 single column, compact stepper, bounded preview, sticky CTA clearing Bottom Navigation (existing PageContainer padding), dialogs fit viewport, long rejection reasons wrap; md+ two-column bounded layout (instructions/trust left, selection+submit right), content max width from existing tokens, no desktop-only workflow.
- Tests: `app/src/features/payment/payment-redesign.quality.test.ts` (deterministic hierarchy/token/copy/geometry/approved-mapping gates), new `e2e/payment-redesign.spec.ts` (20 real-browser scenarios incl. copy, validation, replace/remove, preview, pending, refresh, rejected reason + resubmission, approved, protected preview, expired/unauthorized, duplicate prevention, mobile layout at 7 viewports, dark mode, keyboard flow, no raw backend errors), `e2e/p1-s1.spec.ts` adapted only for the new confirmation step (all prior assertions kept), opt-in `e2e/payment-screenshots.spec.ts` (FEP_SCREENSHOTS=1) writing uninspected artifacts to `/tmp/opencode/fep-payment-redesign/` (390×844 light/dark, 768×1024 light, 1440×900 light/dark × 7 states). Backend payment smokes untouched and green.
- Record: Visual Slice 1: Complete · Visual Slice 2: Complete · Payment redesign: Complete · Operator redesign: Not started · Audio reliability: Not started · Performance: Not started · Monitoring: Not started · Landing redesign: Not started.
- Not performed: Operator redesign, Audio Reliability, Performance, Monitoring, Landing redesign, Backend/API/schema/authz changes, `/review`, commit, push, publish, deploy.

## Operator Workspace Redesign — accepted approach (2026-08-04)
- Scope is client-only: transform the Operator payment-review surface (queue + detail) into an operations workspace. No PocketBase migrations/schemas/routes, authorization, role requirements, status transitions, approval transaction, subscription creation, rejection rules, audit behavior, receipt protection, PWA cache policy, Android or deployment changed. API contracts unchanged: queue `GET /api/fast-english/operator/payment-requests` (page/perPage/status/search only — no sort/plan/date-range params exist, so no such controls are invented; ordering stays Backend-owned), detail route (no per-user cross-request history exposed → the history section shows the bounded lifecycle of the selected request only: submitted/reviewed/activated from existing detail fields).
- Routing: `OperatorRoute` renders one `OperatorWorkspace` for both `/operator` (index) and `/operator/payment-requests/:requestId`; selection navigation preserves the queue's search query so Back returns to the same queue state. `RouteTransition` gets a stable key for `/operator*` so the queue pane stays mounted during selection (filter state, scroll, no refetch on selection); all other routes keep the pathname key. `AppShell` skips its scroll reset for operator paths (the workspace owns scroll; queue scroll is saved/restored on mobile selection). `AppHeader` for operators: Brand (md+), non-heading «پنل اپراتور» title, Logout icon + ThemeSwitch only (no Student nav, no new destinations).
- Responsive model, deterministic by width: `md+` (≥768px, custom breakpoints) = split workspace (queue pane 340px md / 400px lg, sticky queue toolbar, independent pane scrolling inside `calc(100dvh − header − safe areas)`, no nested scroll traps); below md = queue route ↔ full detail with preserved filters + restored scroll; no split below 768px. 768×1024 portrait keeps the compact split (queue 340 + detail ~428).
- Queue: unified keyboard-selectable list of `OperatorRequestItem` (name, masked phone, plan, amount, age/time, status chip with icon+text) replacing the table+cards pair; selected state = `aria-current` + shape indicator bar + tonal background (not color-only); status Select + debounced search (350ms, superseded requests aborted) + clear-search + one «پاککردن فیلترها» action, all reflected in URL query params; heading states the view («نمایش: …») with the Backend `totalItems` count (no client-side counting, no fabricated SLA/fraud indicators); explicit `no-pending` success empty state («درخواست در انتظار بررسی وجود ندارد.») distinct from filtered-empty.
- Detail hierarchy (in order): status+identity (status chip, request ID via `CopyValue`, submitted/updated time, refresh) → User summary (masked phone, account status, placement/level read-only) → payment expectation (plan/amount/duration/bank/transfer) → receipt (`OperatorReceiptInspector`: protected blob route, auto-load, loading/error+retry/missing states, bounded preview ≤360px, accessible zoom via shared `ReceiptZoomDialog` full-screen <sm) → current Subscription (active/latest, read-only) → request history (bounded lifecycle timeline) → decision panel (Approve primary / Reject destructive, ≥44px, separated, only when pending; never before the receipt). Approve/Reject dialogs: confirmation summaries, double-submit lock, busy disables conflicting actions, full-screen on phones (<sm), success only after Backend 200 with authoritative startsAt/expiresAt from the response, internal note on a distinct labeled surface (lock icon, «فقط اپراتورها») — never confused with the public reason (required ≥3 chars, ≤500, visible-to-Student warning, inline validation); after success focus moves to the resulting status region (aria-live).
- Concurrency/stale: no client locking; existing Backend 409 (`request_not_pending`/`approval_conflict`) and idempotent `already_*` kinds drive a `OperatorStaleState` alert + authoritative detail/queue refresh; decision result never shown before Backend acknowledgement. New `operator/errors.ts` maps every failure to stable Persian codes (unauthorized/forbidden/not-found/already-decided/rate-limited/timeout/unavailable) with optional `requestId` rendered only inside safe error details (StatePanel `requestId` slot); raw PB internals never surface.
- Motion: selection/detail-replacement/filter/dialog/success/stale entrances only, from motion tokens; no continuous row animation; reduced-motion collapse inherited. Screenshots are opt-in (FEP_SCREENSHOTS=1) uninspected artifacts under `/tmp/opencode/fep-operator-redesign/`.
- Tests: unit/quality gates (errors mapping, empty-kind derivation, note separation payloads, item structure/selected semantics, split-breakpoint helper) + updated `e2e/p1-s2-qa.spec.ts` (same intent: overflow/RTL/no-bottom-nav/receipt bounds/dialogs/44px/focus ring; table-vs-cards assertions replaced by list+split assertions) + new `e2e/operator-redesign.spec.ts` covering the 30 real-Backend scenarios (login, queue, search/filter, select, user/plan inspection, protected preview+zoom, approve chain + subscription + queue removal, reject validation + note separation + student visibility, two multi-operator races, already-decided refresh, unauthorized student, no Student destinations, mobile/tablet/desktop geometry, dark mode, keyboard-only, long Persian reason, no raw errors). Operator/payment smokes untouched.

## Payment redesign E2E stabilization — complete (2026-08-05)
- Scope: test-only stabilization of the payment E2E suites. No production code, Backend rule, schema, authz, or API contract changed; no assertion weakened; no `.skip`/`.only`/retries added (the CI retry already configured is unchanged).
- Root cause of duplicate Plan fixtures: `e2e/global-setup.ts` spawned the disposable PB on the fixed port with a unique data dir, but when a stale PB from a previous run still owned the port, the new process died silently, the health check succeeded against the stale instance, and the run operated on the previous run's DB. `e2e/global-teardown.ts` only SIGTERMed the recorded pid (the dead new process), so the stale instance survived and accumulated fixtures across runs (p1-s1 created «E2E Monthly» unconditionally → same-named plans → strict-mode `resolved to 2 elements`).
- Fixes: (1) global-setup now refuses to start when another PB already answers on the port (loud error instead of silent reuse); (2) global-teardown now SIGTERM → 3s grace → SIGKILL; (3) new `e2e/fixtures.ts` shared owned-fixture helpers: deterministic upsert — plans named `E2E Monthly <run-suffix>` (unique per creation), owned duplicates (name prefix / `card_number='0000000000000000'` marker) deleted before reuse, exactly one owned plan/destination per DB; specs select the exact plan via `data-testid="plan-<record-id>"` instead of the accessible name. Note: this PB build returns HTTP 400 for `sort=created` (system fields unusable — same quirk as the earlier hooks probe), so the dedupe lists without a sort.
- Legacy `e2e/p1-s1.spec.ts`: adapted to the shared helpers; plan selection by record ID; the caption assertion uses the actual suffixed plan name; resubmit CTA via `resubmit-cta` testid; receipt-preview responsive tests rewritten to bounding-box invariants (frame inside viewport, image inside frame, 1:1 aspect preserved via natural size + `object-fit: contain`, zoom dialog inside viewport, no horizontal overflow, `elementsFromPoint` proof that the fixed Bottom Navigation does not cover the primary action); destination assertion made deterministic (the app displays the first active destination, which is not stable across specs in a full run → assert the real bank row renders instead of a specific bank name).
- `e2e/payment-redesign.spec.ts` / `e2e/payment-screenshots.spec.ts`: same fixture/locator treatment; dark-mode test waits for the details card before reading computed styles; keyboard test presses Enter through the locator (re-focuses at press time) to close the focus race after the plan-selection re-render.
- Results: payment pair 27 passed (workers=1 and normal config), operator trio 66 passed, full suite 259 passed / 0 failed / 3 pre-existing visual-slice flaky (each passed on the configured retry; theme-switch navigation, contrast timing, mini-player sub-pixel) / 10 intentional opt-in skips, exit code 0. `pnpm typecheck`, `pnpm check`, `pnpm test`, `pnpm smoke:payment`, `pnpm smoke:payment-preview`, `pnpm build:app`, `pnpm build:landing`, `bash scripts/verify.sh`, `git diff --check` all pass. No PocketBase process left after runs (verified).
- Record: Visual Slice 1: Complete · Visual Slice 2: Complete · Payment redesign: Complete · Operator redesign: Not started · Audio reliability: Not started · Performance: Not started · Monitoring: Not started · Landing redesign: Not started.
- Not performed: Operator redesign, Audio Reliability, Performance, Monitoring, Landing redesign, Backend/API/schema/authz changes, `/review`, commit, push, publish, deploy.


- Scope: test-only consolidation. No production code, schema, route, authz, payment, PWA-cache, Android or deployment behavior changed.
- Verification tiers (canonical commands):
  - `pnpm verify:fast` — typecheck + Biome + Vitest (everyday gate; ~20s).
  - `pnpm verify:feature [group] [surface]` — verify:fast + affected real Backend Smoke groups (`auth|payment|placement|lessons|progress|all`) + critical Playwright (`--grep @critical`, 17 canonical-journey tests) + affected build (`app|landing|all`).
  - `pnpm verify:full` — project-verify.sh (fast + all 10 smoke suites + deterministic builds + topology + PWA output + Android version/signing fail-safe + deploy redaction proofs) + full Playwright. `scripts/verify.sh` delegates here; CI runs it (timeout raised to 30 min, Chromium installed by ci-install.sh).
- Layer ownership: Vitest = pure logic/schema/format/state-machine/design tokens + static guards for high-risk rules (forbidden hex/durations, cross-surface build isolation, receipt-privacy contract, approved-panel static guard). Real Backend Smoke = all routes/authz/payment/idempotency/placement/progress/protected files (parameterized matrices). Playwright = browser-required behaviors only; the 17-test `@critical` project covers the 8 canonical journeys (signup/login, payment pending, operator approval+activation, placement+level selection, lessons/audio/progress, unauthorized/expired denial, theme+responsive shell, PWA private-cache boundary).
- Consolidations:
  - Viewport matrices reduced from 6–7 widths to the routine set (390×844, 768×1024, 1440×900) in p1-s1, p1-s2-qa, p2-qa, p3-s2, payment-redesign, visual-slice-1/2; dedicated 360px layout tests kept (entry/signup/player/app-bar/dialog-at-360, p1-s1 mobile test). Playwright 341 → ~237 tests.
  - Smoke fixtures centralized into `scripts/smoke-common.mjs` (fetchJson/nextPhone/randomId/superuser/operator/login/createActiveStudent) — removed ~700 lines of verbatim duplication across smoke-placement, smoke-placement-levels, smoke-lessons, smoke-progress, smoke-operator.
  - smoke-placement-levels: removed speculative 429-retry backoff sleeps and the artificial 2s pacing between gate scenarios (probes showed no transport-level 429s; PB settings rate limiting is disabled mid-suite) — 48s → ~18s.
  - Vitest: removed one proven-duplicate assertion (api.dup network-failure mapping, already covered by api.test).
  - Flakiness causes handled: p3-s2 beforeAll idempotent fixture guard (worker-restart duplicate seeding) retained; no retries added anywhere.
- Record: Vitest 32 files/423 tests (~14s), smokes 10 suites (~76s), Playwright ~237 tests (~14 min), verify:full ~16 min. Baseline was Playwright 341 tests (~22.1 min), smokes ~107s.

## Podcast Slice 1 — Student/Admin separation + unified Staff Admin Console (2026-08-05)
- **Goal:** one public Student App, one unified Staff Admin Console, one backstage identity (`staff_admins`), no roles/permissions matrix. Only the existing payment-management workspace was migrated; Podcast content, imports, publishing, analytics and student management are later slices.
- **Topology:** three surfaces, one repository, one lockfile: `landing/`→`dist-landing`, `app/`→`dist-app`, `admin/`→`dist-admin` (new `vite.admin.config.ts`, `pnpm dev:admin/build:admin/preview:admin`; `pnpm build` builds all three). Caddy serves `admin.fastenglishpodcast.com` from `current/admin` with its own SPA fallback (no Student bundle, no `/operator` redirect, no PWA artifacts); deploy.sh + `check-production-bundle.sh` verify `admin/` in the bundle.
- **Shared code (`shared/`):** design tokens, MUI theme (one implementation), Brand + assets, RTL cache, ThemeSwitch, PageContainer/StatePanel/PageHeader, CopyValue, ReceiptZoomDialog, `shared/lib/formatters.ts` (formatFileSize), `shared/lib/apiOrigin.ts`. Admin never imports Student feature code; build-boundary proof in `scripts/check-bundle-boundaries.mjs` + `shared/build-boundary.test.ts`.
- **Staff Auth Collection:** `server/pb_migrations/1700000017_create_staff_admins.js` (auth; `display_name` + `is_active`; all five rules `null`; email required/unique identity; no `role` field; no manage rule; password reset disabled until approved SMTP — documented operational state). `1700000018_relink_staff_reviewers.js` retargets `payment_requests.reviewed_by` + `subscriptions.approved_by` to `staff_admins` (remove+re-add, two saves — PB forbids in-place relation retargeting).
- **Guards:** `server/pb_hooks/guards.pb.js` defines exactly `requireStudent` (fep_users + role student, rejects legacy staff), `requireActiveStudent` (+ active account), `requireStaffAdmin` (staff_admins + is_active). PB 0.39 routerAdd closures cannot see hook-file globals, so route handlers load the module with `require(__hooks + '/guards.pb.js')` (empirically verified; bare/relative `require` paths fail with "Invalid module"). All staff routes (renamed `staff_routes.pb.js`, API paths preserved) use requireStaffAdmin + `requireAuth("staff_admins")`; student routes use requireStudent/requireActiveStudent; the audio route's file-token path re-checks the same rule on the resolved record (no `e.auth`).
- **Bootstrap + diagnostics:** `pnpm staff:bootstrap [--replace]` (FEP_PB_URL, FEP_PB_SUPERUSER_EMAIL/PASSWORD, FEP_STAFF_EMAIL/PASSWORD/DISPLAY_NAME; fails on missing/weak values; never creates a second record silently; sets active+verified; prints no secrets) and `pnpm staff:diag [--ids]` (legacy non-Student fep_users count only; IDs only with `--ids`; no phones/emails/tokens). `staff_admins` auth hooks reject inactive/unverified records (login + refresh). Legacy fep_users operator tokens are rejected by both staff and student routes.
- **Admin Console:** `admin/` SPA — routes `/login`, `/`, `/payments`, `/payments/:requestId`, `/settings`, Not Found; StaffAuthProvider with a dedicated PocketBase client + `LocalAuthStore('fep_staff_auth')` (isolation proven by `shared/authstore-isolation.test.ts`); dashboard shows only the real pending-request count; settings holds the only theme control («ظاهر»); the migrated operator workspace lives at `admin/src/features/payments/` with Admin copy («درخواستهای پرداخت», «ورود مدیریت», «داشبورد مدیریت»); deep links survive refresh; no Service Worker/manifest (PWA stays Student-only).
- **Student cleanup:** `/operator` removed (routes, guard kind, header links, shell branches, RouteTransition operator key); unknown paths render a Student-safe Not Found (including `/operator`, `/admin`, `/staff`); theme control removed from the Top App Bar, Entry and auth pages — it exists only in Account settings («تنظیمات نمایش») and Admin settings; payment copy reworded to public-product language (no «اپراتور» anywhere in the Student surface).
- **Verification:** `scripts/project-verify.sh` adds `smoke:staff` (schema, locked rules, cross-token matrix, bootstrap fail-safes, legacy diagnostic), `build:admin`, dist-admin topology + no-PWA assertion, Student/Admin bundle boundaries. New `e2e/student-admin-boundary.spec.ts` (15 tests) covers the 18 focused scenarios; p1-s2 / p1-s2-qa / operator-redesign suites now run against the Admin Console; visual-slice theme tests drive the preference through Settings; all operator fixtures in e2e/smokes use `staff_admins` via the disposable superuser (`e2e/fixtures.ts createStaff`, `smoke-common.mjs getStaffToken`). Playwright full suite: 275 passed / 0 failed / 2 documented visual-slice flakes (each passed on the configured retry) / 10 opt-in skips, exit 0. Unit: 497. Smokes: auth 41, payment 61, payment-preview 16, operator 110, staff 48, placement/levels/lessons/progress/race/capacity all green. `pnpm typecheck`, `pnpm check`, `pnpm test`, builds, `scripts/verify.sh`, `scripts/project-verify.sh`, `git diff --check` all pass.
- Record: Podcast Slice 1: Complete · Podcast Slice 2: Not started · Podcast Slice 3: Not started · Podcast Architecture Review: Pending after Slice 3. Visual Slice 1/2 + Payment redesign: Complete (preserved). Student/Admin boundary: Complete · Unified Staff Admin foundation: Complete · Theme preference in Settings only: Complete · Podcast content domain: Not started · JSON content pipeline: Not started.
- Not performed: Podcast categories/episodes/library/vocabulary/JSON import/Content Studio/analytics/audio reliability/performance work, landing redesign, Production Staff bootstrap (no real Staff account exists yet), Production deployment, `/review`, commit, push, publish.

## Podcast Slice 2 — Podcast content domain (2026-08-05)
- **Goal:** evolve the Topic/Lesson model into a production-ready Podcast domain (Categories, Episode metadata, level variants, vocabulary, cross-level browsing, backward-compatible migrations, preserved Progress). Database collection names stay stable (`categories`, `topics`, `lessons`, `lesson_vocabulary`, `lesson_progress`); no `episodes`/`episode_variants`/`episode_progress` collections; no `topics`/`lessons` renames.
- **Migrations:** `1700000019_create_categories` (locked CRUD; key/slug unique; `publication_status` enum draft/published/archived; published requires title_fa/slug/description_fa; indexes key/slug/(publication_status, sort_order)); `1700000020_upgrade_topics_to_episodes` (content_key unique, category relation, title_fa, description_fa, artwork_square + hero_image_wide (JPEG/PNG/WebP ≤5 MB, thumbs 640x0/1280x0), artwork_alt_fa, episode_number, is_featured, content_version; indexes content_key (U) + (category, status, published_at)); `1700000021_upgrade_lessons_to_variants` (summary_fa, thumbnail_override, thumbnail_alt_fa, content_version; index (status, published_at)); `1700000022_create_lesson_vocabulary` (locked; unique (lesson, normalized_term), (lesson, sort_order); protected pronunciation audio MP3/M4A ≤2 MB); `1700000023_backfill_podcast_domain` (default Category general/عمومی published; `content_key = legacy.<slug>`; content_version=1; lessons versioned; lesson_progress untouched).
- **Publication model:** topics/lessons already had a valid `status` enum — reused as the single authoritative publication field (no `publication_status`, no Boolean). Categories use `publication_status` (new collection, recommended name). New-field invariants (published Episode: category/content_key/title_fa/description_fa/artwork/content_version; published Variant: summary_fa/content_version) are enforced on create-with-published and on transition INTO published only — **grandfather-until-republished** for legacy content, because PB 0.39 hooks see no `originalRecord` during migration/import saves (a discovered constraint; documented in docs/PODCAST_DOMAIN.md). Published Variant additionally requires a published parent Category (unconditional, mirroring the topic check). Archive hides content but never deletes Progress; republish restores the same Progress. PB 0.39.9 quirk verified from pinned source: required NumberFields reject 0 («cannot be blank») → new sort_order fields are non-required with hook defaults.
- **Centralized helpers:** `server/pb_hooks/podcast_domain.pb.js` (loadable via `require(__hooks + '/podcast_domain.pb.js')` from routerAdd closures AND from model hooks — globalThis does NOT share across hook-file runtimes in 0.39, discovered empirically): CEFR_ORDER, normalizeLevel, getRecommendedLevel (suggested_level → attempt fallback), getPreferredLevel (selected_level when valid → recommended), requirePublishedCategory/Topic/NewVariantInvariants, listPublishedLevelsForEpisode (CEFR order), resolveEpisodeArtwork, normalizeVocabularyTerm, artwork byte-serving core.
- **Level semantics:** recommended = Placement result (`suggested_level`, never changed by browsing); preferred = existing `selected_level` (valid) else recommended (documented mapping — no new field); browsing = read-only `?level=` param on the lesson list (validated A1–C2, default preferred, invalid 400); entitlement = any Published Variant A1–C2 (level equality removed from list/detail/audio/progress GET+PUT; subscription/account/placement/published-state/audio-token/revision protections preserved).
- **API:** lesson list/detail extended (legacy fields preserved): `episode` (id, slug, contentKey, title, titleFa, descriptionFa, category, artwork, heroImage, featured), `variant` (id, level, summaryFa, transcript, audioDurationSeconds, publicationStatus), recommendedLevel, preferredLevel, availableLevels (published-only, CEFR order, isRecommended/isPreferred flags), vocabularyCount (detail only — avoids list N+1). New public artwork proxy `GET /api/fast-english/artwork/{lessonId}` (+`/hero`): serves only when Variant+Episode+Category published, `public, max-age=3600`, nosniff, containment, 5 MB cap; resolution order thumbnail_override → artwork_square → deterministic branded SVG fallback (legacy content). Public sample routes now require a published Category; Dashboard/summary/continue filter by published Category and stay preferred-level scoped.
- **Client:** `shared/podcast/domain.ts` (pure mirror: CEFR order, normalizeLevel, recommended/preferred fallback, vocabulary normalization, artwork fallback, legacy content-key mapping, publication-state helper) + `app/src/features/podcast/podcast-domain.test.ts` (18 tests) + `podcast-static-guards.test.ts` (11 static gates: no duplicate semantic fields, no deprecated publication Boolean, no UI-side browsing mutation of preferred level, no file-path leakage in response types). lessons types/api extended (optional episode/variant/availableLevels; `getLessonList(page, perPage, level?)`).
- **Verification:** `scripts/smoke-podcast-domain.mjs` (47 scenarios; Part 1 is a genuine migration test — a disposable PB started with only pre-slice migrations + raw legacy topics/lessons/progress seeded with NO hooks, then restarted with the full migration set: default Category created, content_key=legacy.<slug>, version 1, links and Progress counts byte-identical, grandfathered content visible, artwork fallback SVG; Part 2 covers the 28 required domain scenarios). Wired into package.json (`smoke:podcast-domain`) and project-verify.sh. New `e2e/podcast-domain.spec.ts` (8 tests). Fixture helpers in smoke-lessons/smoke-progress/p3-s1/p3-s2/visual-slice-2 updated to the publish invariants (draft → artwork → publish; summary_fa + content_version); wrong-level assertions flipped to cross-level (smoke-lessons, smoke-progress, p3-s1 test 2). Unit: 555. Full suite + canonical verification in the final slice report.
- Record: Podcast Slice 1: Complete · **Podcast Slice 2: Complete** · Podcast Slice 3: Not started · Podcast Architecture Review: Pending after Slice 3. Categories: Complete · Episode domain: Complete · Level variants: Complete · Vocabulary schema: Complete · Cross-level entitlement: Complete · JSON content pipeline: Not started · Library UI: Not started · Episode UI redesign: Not started.
- Not performed: Library UI, Episode page redesign, Vocabulary UI, JSON package import, Admin Content Studio, Audio background controls, Performance, Monitoring, Analytics, Landing redesign, Production deployment, `/review`, commit, push, publish. The Podcast Student experience is not complete.

## Podcast Slice 3 — Content Package pipeline (2026-08-06)
- **Goal:** make adding a Podcast Episode simple enough for one backstage administrator: template → assets → local validation → dry-run plan → Staff-authenticated import as Draft → later preview/publish. The same package contract is reusable by the future Unified Admin Console; no Admin Content Studio/Import UI is built in this slice.
- **Transport decision (proven):** single Staff-authenticated multipart request (`manifest` string part + one part per declared asset path) executed in one `$app.runInTransaction`. The required architectural probe ran against the pinned 0.39.9 binary and passed 14/14: multipart manifest+files received intact (`e.request.parseMultipartForm` must run first — `e.request.form` is empty otherwise), files assigned to Topic/Lesson records (`rec.set(field, f)` after `$security.randomString` rename), records+files committed atomically, and a forced unique-index failure rolled back records AND files (storage dir byte-identical after). Probe findings encoded: goja rejects `0` on required NumberFields (`sort_order` must be ≥1), JSON bodies need UTF-8 decoding (`String.fromCharCode` mojibakes Persian; `$apis.requestInfo` does not exist in 0.39.9 — read via `toBytes(e.request.body)`).
- **Specification:** `schemas/episode-package.schema.json` (draft 2020-12, Ajv 2020, `additionalProperties: false` everywhere, unknown-property rejection). `schemaVersion 1.0.0`; `contentKey = <categoryKey>.<slug>`; one Episode; 1–6 unique CEFR Variants; vocabulary with required term/meaningFa/definitionEn; asset paths package-relative only. Manifest never embeds media (no Base64).
- **Template:** `pnpm content:new <slug> [--levels A1,B1,C1]` creates the package skeleton (B1 default) with `TODO_REPLACE` placeholders that intentionally fail strict validation; refuses to overwrite; prints next commands; never touches the database.
- **Local validation (CLI):** schema → identity → path safety (structural + realpath containment; symlink escape rejected) → asset inspection (image-size for geometry; music-metadata + deterministic MP3-frame/M4A-mvhd parsers for authoritative duration; transcript UTF-8 normalization with BOM/CRLF/blank-line handling and forbidden-construct rejection) → editorial diagnostics (documented thresholds; no AI) → per-asset SHA-256 → deterministic package fingerprint (sorted assets, canonical manifest; independent of paths/timestamps/ordering). Pure core in `shared/content-package/*` (browser-safe, typechecked, 48 unit tests) + Node-only `scripts/content/*.mjs` (fs-bound, 19 unit tests incl. CLI exit codes).
- **Server hooks:** `content_import_core.pb.js` (mirrors SHA-256/fingerprint/duration parsers — smoke asserts CLI/server parity) + `content_import_routes.pb.js` — `POST /staff/content-import/plan` (read-only diff + `planStateHash` stale-plan token), `POST /staff/content-import/execute` (full server-side revalidation: identity, paths, sizes, signatures, durations, Category existence, version rules, duplicate vocabulary, unexpected parts; one transaction; audit), `GET /staff/content-imports/{id}` (sanitized audit). Guards: `requireStaffAdmin` + `requireAuth("staff_admins")`; Students/legacy Operators/unauthenticated rejected; per-staff rate limits.
- **Version rules:** new key → create as Draft; same key+version+fingerprint → `no_change` (no writes); same version+different fingerprint → 409 conflict; higher version → update into Draft (published Episode + ALL its Variants move to draft so stale content can never resurface on republish; imported variants get new content/vocabulary; existing Progress untouched); lower version → 409 stale. Category must already exist (no silent creation).
- **Audit:** new locked `content_imports` collection (content_key, content_version, package_fingerprint, schema_version, status planned/running/completed/failed/no_change, bounded sanitized summary_json/error_json, imported_by → staff_admins, started_at/completed_at) with a partial unique index `(content_key, content_version, package_fingerprint) WHERE status='completed'` — failed attempts never block corrected retries; audit writes sit outside the content transaction so failures are always recorded (a crash leaves `running`, superseded as failed by the next execute).
- **CLI:** `content:validate` (PASS/FAIL, errors/warnings, media, durations, fingerprint; exit 0/1/2), `content:plan [--json]` (Staff auth via FEP_PB_URL/FEP_STAFF_EMAIL/FEP_STAFF_PASSWORD; deterministic text plan; zero mutation proven), `content:import [--yes]` (validate → plan → explicit confirmation → execute → safe result). No secrets printed; exit codes documented.
- **Verification:** `pnpm smoke:content-import` 32/32 scenarios (validation, template failure, zero-mutation plan, CLI Draft import, category reuse, two variants, vocabulary order, durations, artwork, public-hide-draft, no-change + no_change audit record, execute-without-planStateHash rejection, version-filtered fingerprint idempotency v1→v2→v2, oversized-vocabulary rejection, conflict, higher-version draft update, stale rejection, rollback with no orphan files, no partial state, retry-after-failure, progress unchanged, audit completed + sanitized failed diagnostics, student/inactive-staff/legacy rejection, unsupported media, traversal + symlink rejection, no orphans). Focused browser boundary `e2e/content-import-boundary.spec.ts` (4 tests: staff plan route, student denied, Admin app functional, Admin SPA session calls plan) + `student-admin-boundary` — 19/19 green. Unit: 598 (incl. 48 content-package + 21 pipeline + 3 env-hygiene). `smoke:podcast-domain` 47/47; staff/lessons/progress/payment/operator/auth smokes green; project-verify.sh extended (smoke:content-import, schema JSON parse, example package PASS).
- Record: Podcast Slice 1: Complete · Podcast Slice 2: Complete · **Podcast Slice 3: Complete** · Podcast Architecture Review: Ready. Content Package specification: Complete · JSON Schema: Complete · Template generator: Complete · Validation: Complete · Dry-run planning: Complete · Draft import: Complete · Import idempotency: Complete · Import audit: Complete. Unified Admin Content UI: Not started · Library UI: Not started · Episode UI redesign: Not started · Vocabulary UI: Not started.
- Not performed: Library UI, Student Home redesign, Episode/Vocabulary UI, Device TTS, Admin Content Studio, Admin Import screen, Publishing UI, Audio background controls, Monitoring, Analytics, Landing redesign, Production deployment, `/review`, commit, push, publish. Imported Draft content is not publicly visible.

## Podcast Slice 4 — Content Studio in the Unified Admin Console (2026-08-07, accepted approach)
- **Goal:** turn the Admin app into the practical operational interface for Podcast content: Categories, Episodes, six Level Variants (Summary/Audio/Transcript/Vocabulary), Artwork, authoritative readiness, Draft preview, Publish/Archive, and the Content Package ZIP import UI — all served by bounded Staff routes on the existing domain + import core. One Staff Administrator; no roles/permissions/approval chains.
- **ZIP ingestion decision (proven constraint):** the pinned PB 0.39.9 JSVM exposes NO zip/inflate module (probed live: `require('zip')`, `archive/zip`, `compress/zlib`, `jszip` all → "Invalid module"; no TextDecoder/atob). A server-side inflate would require a ~400-line pure-JS DEFLATE mirror in goja — high-risk, out of budget. Accepted approach: a **bounded browser-side ZIP adapter** (`shared/content-package/zip.ts`, pure TS; central-directory parser; stored + deflate via the built-in `DecompressionStream('deflate-raw')`; rejects traversal, symlinks, unsupported methods, >64 entries, total uncompressed >60 MB, per-entry asset caps) converts the ZIP into **exactly the Slice 3 internal representation** (manifest + asset parts) and feeds the existing `POST /staff/content-import/plan` + `execute` routes unchanged. The server remains the full security boundary: it re-validates manifest structure, every part's path/size/signature/MIME, durations, fingerprints and the planStateHash — the browser ZIP parser is transport convenience, never authority. No second schema; CLI/UI parity asserted by smoke.
- **Server:** new `server/pb_hooks/content_admin_routes.pb.js` — bounded Staff routes (requireStaffAdmin + requireAuth): categories list/create/edit/publish/archive/feature/reorder/cover; episodes list/create/get/edit/publish/archive/feature + square-artwork/hero upload+remove; variants create/get/edit (summary_fa)/audio upload+remove (server-extracted authoritative duration)/transcript save/publish/archive; vocabulary create/update/delete/reorder/pronunciation upload+remove; authoritative readiness per Episode+Variant (errors block publish; warnings don't; legacy-published content reads as ready with an explicit "republish will require these fields" warning — grandfathering preserved); staff-only Draft preview (sanitized representation); staff-only media routes for artwork/audio/pronunciation (Range for audio seek, nosniff, containment — no storage names in responses); recent import history list. New minimal `content_operations` audit collection (publish/archive/media-replacement/import-related ops; bounded detail_json; performed_by staff_admins) — no event-sourcing.
- **Readiness semantics:** per-episode and per-variant `{ ready, legacy, errors[], warnings[] }`. Episode errors: category not published, missing slug/content_key/content_version/title_fa/description_fa/artwork. Variant errors: missing body/audio/positive duration/summary_fa/content_version (and parent Episode/Category not published for a publish action). `legacy=true` only when the record is currently published with missing new fields → no errors, warning lists what republish will require. Publishing routes re-run the domain invariants (main.pb.js hooks) and return clear Persian-safe errors.
- **Admin:** `admin/src/features/content/` feature (api/types/errors/readiness/plan-presentation/vocabulary-batch/unsaved-state + components) and routes `/content` (dashboard), `/content/categories`, `/content/episodes`, `/content/episodes/new`, `/content/episodes/:episodeId` (tabs: اطلاعات اصلی / تصاویر / نسخههای سطح / پیشنمایش / انتشار), `/content/episodes/:episodeId/variants/:level` (خلاصه / صوت / متن اپیزود / واژگان / انتشار), `/content/import` (ZIP → validation report → dry-run plan → confirm → execute → result + history), `/content/preview/:episodeId`. Nav: داشبورد / محتوا / ورود محتوا / پرداختها / تنظیمات. Theme stays Settings-only. Explicit Save + dirty-state route blocker (no aggressive beforeunload), no autosave-to-PocketBase per keystroke.
- **Verification:** `pnpm smoke:content-admin` (28 scenarios, disposable owned records), focused unit tests (readiness presentation, error copy mapping, variant completeness, plan presentation, no_change, stale-plan copy, unsaved-state, CEFR order reuse, no duplicated constants, ZIP parser), `e2e/content-studio.spec.ts` (manual creation, ZIP import, invalid package, stale plan, security, responsive 390/768/1440) + targeted regressions + one Full Suite, then canonical verification (§42). No commit/push/deploy//review.

## Podcast Slice 5 — Podcast-first Student App: navigation, Home, Product copy (2026-08-08, accepted approach)
- **Goal:** the Student App stops feeling like Dashboard → Lessons → Account and begins feeling like a Personal English Podcast App (Discover → Listen → Understand → Continue). Final navigation architecture, redesigned Home, the Product copywriting system, and the reusable visual/content foundations (Episode Card, artwork, content sections) that Slice 6 (Library) and Slice 7 (Episode) build on. The full Library, Episode page redesign, Vocabulary UI, Level Switcher, Media Session, background audio, analytics and monitoring are NOT implemented here.
- **Final Student IA:** phone Bottom Navigation / tablet Navigation Rail / desktop Side Navigation all carry exactly خانه (Home at `/`) → کتابخانه (`/library`, transitional) → پیشرفت (`/progress`, level-scoped summary) → حساب (`/account`). Legacy `/dashboard` redirects to `/` (PWA shortcuts keep working); `decideRoute` redirect targets updated; the old dashboard feature directory is removed.
- **Routing/host change:** the shared `PlayerProvider` moved above all routes so Home (rendered as `<AppShell><HomeRoute/></AppShell>` via the new `RootGate` at `/`) keeps the single-audio-element invariant; the provider now stops playback + clears the session when the auth session disappears (logout) — otherwise audio would keep playing with no Mini Player on public pages. `AppShell` renders children or the Outlet; guests still get the Entry route at `/`.
- **Home composition (answers 1) listen now 2) relevant 3) progress 4) account):** compact greeting header («سلام [name] / امروز چی گوش میدی؟», name only when reliable); Continue Listening hero (dominant when real resumable progress exists — artwork, category, titleFa, level, authoritative duration, saved-position CTA «ادامه از HH:MM», remaining minutes only from duration−position, progress bar); first-use start experience («اولین اپیزودت را شروع کن» + «پیدا کردن اپیزود» → /library) when nothing was ever started; completion state when all completed; honest unavailable state when started Episodes are no longer published; «مناسب سطح شما» (preferred-level, featured-first, 3 items); «تازه منتشر شده» (real published_at, 3 items, no fake analytics); one compact progress panel (started/completed-of/completion%, preferred level, «سطح پیشنهادی: X — در حال مرور: Y» when they differ — both represented without judgment, neither value ever changed by rendering); quiet compact subscription line (no payment-style card, no actions). Media-aware `HomeSkeleton` replaces the dashboard skeleton; errors carry the next step + Retry.
- **Level semantics preserved:** Home uses the server-preferred level (selected_level → suggested_level fallback) for Episode sections; the Placement result (recommended) is only presented, never changed; per-request `?level=` browsing on the lessons API is read-only (proven by e2e).
- **Reusable foundations (`app/src/features/podcast/components/`):** `EpisodeArtwork` (fixed 1:1, token radius, object-fit cover, server-resolved artwork → controlled Product fallback on error, meaningful alt); `EpisodeCard` (artwork/category/title/level/duration/progress/one semantic action; `auto` layout = compact row on phones + standard vertical card md+, `row` = Library rows; states reuse the deterministic lessonCardState derivation with Product CTA copy شروع گوشدادن / ادامه از HH:MM / مرور دوباره); `ContentSection` (shared heading row with wrap — headings never collide with section actions).
- **Product copy system:** `app/src/app/copy/productCopy.ts` centralizes the canonical vocabulary (اپیزود، کتابخانه، سطح پیشنهادی، سطح پیشفرض، ادامه گوشدادن، شروع گوشدادن، مرور دوباره، کلمات کلیدی، متن اپیزود، پیشرفت) + repeated actions/status/empty/error copy; `copy-guidelines.md` documents the voice rules and the scanner exceptions. New static gate `podcast-slice-5.quality.test.ts` scans the redesigned Podcast-facing files for outdated terms (درس / اپراتور / پنل / record / PocketBase / operator) and asserts the final navigation/router structure. Legacy flows (payment, placement, the current lessons list page) keep their own copy and are NOT scanned.
- **Account polish:** IA now حساب کاربری (header identity) → اشتراک → سطح زبان → تنظیمات نمایش → خروج; removed the leftover "next slice" placeholder panel; Theme control is a labeled segmented group (سیستم/روشن/تاریک with the documented «هماهنگ با تنظیمات دستگاه» caption) — aria-labels unchanged so existing e2e keep working; theme exists only in Settings everywhere.
- **Progress route (`/progress`):** level-aware summary from the existing level-scoped endpoint (never aggregates cross-level activity), Product vocabulary, no new analytics.
- **Verification:** unit 658+; new `e2e/podcast-home.spec.ts` (23 tests: the 15 §34 scenarios + §33 deterministic geometry at 360/390/430/768/1024/1440 — overflow, artwork containment, CTA visibility, bottom-nav clearance, heading/action non-collision, long-title wrapping, theme placement, no Staff UI); visual-slice-2 (68), visual-slice-1 (41, incl. the documented contrast-timing flake fixed with a settle wait — not weakened), student-admin-boundary, p1-s1, payment-redesign, p2-s2, p2-qa, p3-s1, p3-s2, podcast-domain all green; smokes podcast-domain 47/47, lessons 71/71, progress 69/69; full Playwright 324 passed / 0 failed / 3 documented flakes (each passed on retry: content-studio-screenshots opt-in, login keyboard focus timing, mini-player sub-pixel) / 11 opt-in skips, exit 0; project-verify.sh (all 10 smoke suites + builds + topology + PWA + Android + deploy redaction proofs) green; canonical gate (typecheck, check, test 658, smoke:auth, build:app/admin/landing, scripts/verify.sh, git diff --check) green.
- Record: Podcast Slice 1: Complete · Podcast Slice 2: Complete · Podcast Slice 3: Complete · Podcast Architecture Review: ACCEPT · Podcast Slice 4: Complete · **Podcast Slice 5: Complete** · Student Podcast Shell: Complete · Student Home: Complete · Product Copy System: Complete · Podcast Slice 6 — Library: Not started · Podcast Slice 7 — Episode Experience: Not started · Podcast Slice 8 — Vocabulary: Not started · Student Podcast Experience Review: Pending after Slice 8.
- Not performed: full Library, Category browsing, Library Search, final Episode Detail, Level Switcher interaction, Vocabulary UI, pronunciation, Media Session, background audio, analytics, monitoring, Landing redesign, Admin Content Studio changes, `/review`, commit, push, publish, deploy.

## Podcast Slice 6 — Production Library & Discovery (2026-08-08)
- **Goal:** replace the transitional `/library` route with the production Podcast discovery experience: Browse Topics → Search → Filter → find one canonical Episode → resolve the right CEFR Variant → start/continue listening. The Library is a discovery product, not an LMS catalog.
- **Server (new `server/pb_hooks/library_routes.pb.js`, `GET /api/fast-english/library`):** dedicated endpoint justified by the distinct contract (canonical grouping + Variant resolution + per-Variant Progress merge + filters + deterministic ordering + pagination). Full Student entitlement identical to the lesson routes; Staff/legacy tokens denied; per-user rate limit 120/5 min; `private, no-store`. Bounded params: `q` ≤60 (search title/title_fa/description_fa via PB bound params), `category` (published id; unknown/archived → empty), `level` (`preferred`/`all`/A1–C2), `progress` (`all`/`not_started`/`in_progress`/`completed` of the resolved Variant), `sort` (`suggested`: featured → preferred-compatibility → sort_order → published_at → content_key; `latest`: published_at desc → content_key), `page` 1–50 / `perPage` 1–50 clamped. Publication filtering (Category+Episode+Variant published) runs BEFORE pagination. Response: published Categories with Episode counts, one canonical Episode item per Topic (episode meta + full availableLevels in CEFR order independent of the filter + resolvedVariant with progress{state,percent,positionSeconds,completed}), bounded Continue rail (≤3 in-progress, never completed, archived never resumable), page/perPage/totalItems, recommendedLevel/preferredLevel. Variant resolution: explicit level → that published Variant; otherwise preferredLevel → recommendedLevel → first published CEFR. Browsing is read-only (recommended/preferred levels never mutated — server proof in the smoke).
- **Client (`app/src/features/library/`):** types/api + pure `queryState.ts` (URL-backed `/library?q&category&level&progress&sort&page`, tolerant parse, defaults omitted, bounds mirrored) + pure `logic.ts` (maps the resolved Variant onto the Slice 5 EpisodeCard contract incl. per-Variant Progress; derives the distinct empty-state kind). Components: SearchField (trim/bound/clear/Enter, debounced 400 ms — no request per keystroke), OptionChips (category/level/progress; horizontal scroll on phones; selected = shape+icon+aria-pressed, never color-only), ContinueStrip (restrained horizontal rail), LibrarySkeleton (media-aware), EmptyPanel (per-kind copy + real next action; no generic «no data»). LibraryRoute: h1 + subtitle, search, categories, Continue rail, level/progress/sort filters, results count, EpisodeCard rows (one per Episode; `eager` artwork for the first 3 cards, lazy below), load-more accumulation (page refetch reuses fetched pages; Back/refresh preserve state; race-guarded; keep-list-while-refreshing with a quiet progress line). EpisodeCard/EpisodeArtwork minimally extended (optional `artworkLoading`, optional `availableLevelsCaption`). Product copy extended in `productCopy.ts` (subtitle «موضوع مورد علاقهات را پیدا کن و اپیزود بعدیات را انتخاب کن.», جستجو/همه موضوعها/پیشنهادی برای من/همه سطحها/مرتبسازی/اپیزودهای بیشتر + 5 distinct empty states); the scoped copy scanner now covers the Library files.
- **Verification:** `pnpm smoke:library` 27/27 (new `scripts/smoke-library.mjs`, wired into project-verify.sh step 13f): canonical one-item-per-Episode, published-only Categories/Episodes/Variants, publication-before-pagination, Persian+English search + bounds, Category/archived-Category, explicit Level + fallback chain (preferred → recommended → first CEFR), Progress filter + per-Variant isolation, CEFR availableLevels order, deterministic pagination/sort, Continue rail cap, expired/pending/Staff/legacy denial, param bounds, sanitization, read-only levels. New unit suites `queryState.test.ts` + `logic.test.ts` (24 tests) + `resolveVariantLevel` pure-mirror tests in `podcast-domain.test.ts` (5). New `e2e/podcast-library.spec.ts` (27 real-browser tests, 2 tagged `@critical`): heading/search/categories, one card per multi-level Episode, draft/archived absence, Category filter + URL, search + clear + combined-empty, Level filter + empty state, Progress filter + B1↛A1 isolation («ادامه از 2:30»), Continue rail, deterministic sorts, load-more, correct Variant navigation, Back/refresh state, level fields unchanged, keyboard flow, console/network cleanliness (artwork per-IP 429 + fallback-404 e2e artifacts allowlisted), 200% zoom containment, keyboard visible-focus (`:focus-visible`) + tab traversal, browser-exercised error state + Retry recovery, 6-viewport geometry sweep (360→1440: overflow/artwork bounds/CTA/bottom-nav clearance/filters on-screen/title wrap), Light/Dark. Rapid filter-click race (React Router `setSearchParams` closing over stale params) fixed by making the query component state (functional setState) with URL persistence; two consecutive full-suite runs 27/27. Home regression: podcast-home 21/22 on the fast lane (the 1 failure is the documented pre-existing «theme-switch navigation» flake — ERR_INSUFFICIENT_RESOURCES on full reload after the image-heavy Home) and 23/23 on the canonical lane (CI=1, built app, retry 1) — unrelated to this slice. Unit 687, typecheck clean, `biome check .` unchanged at 20 documented pre-existing findings (none in new files). Reviewer (independent, read-only) verdict: A1–A5, A7 PASS; A6 initially UNPROVEN (zoom/focus/error-state evidence missing) → the three evidence gaps were added as e2e tests 18–20 and now PASS; episodeCount made consistent with discoverable Episodes; dead label export removed and filter labels centralized in productCopy; vacuous smoke assertion replaced; `verify:feature` smoke-group wiring intentionally NOT changed (verification-script policy) — the Feature Lane for this slice runs `smoke:library` explicitly and the two `@critical` Library tests join the feature lane's browser grep.
- Record: Podcast Slice 1: Complete · Podcast Slice 2: Complete · Podcast Slice 3: Complete · Podcast Architecture Review: ACCEPT · Podcast Slice 4: Complete · Podcast Slice 5: Complete · **Podcast Slice 6: Complete** · Library & Discovery: Complete · Podcast Slice 7 (Episode Experience + Vocabulary + Pronunciation): Not started · Student Podcast Experience Review: Pending after Slice 7.
- Not performed: Episode-detail redesign, Level-switcher inside detail, Vocabulary UI, pronunciation, Media Session, background audio, favorites/playlists/downloads/comments/ratings, AI recommendations, trending, analytics, Admin Content changes, Landing changes, `/review`, commit, push, publish, deploy.
