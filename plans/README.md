# Implementation Plans

Generated by the improve skill on 2026-08-14 against commit `f64288e`
(full-audit run: correctness, security, performance, tests, tech debt,
dependencies, DX, docs, direction — all findings vetted by the advisor by
reading the cited code; subagent line numbers were treated as leads, not
facts).

**Default selection**: no interactive selection was available, so the top 5
findings by leverage (impact ÷ effort × confidence, security/gate-integrity
tiebreakers) were planned. Additional plans (e.g. 006-Caddy-import-body-cap,
007-staff-queue-batching, 008-onboarding-docs) can be written on request.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Harden custom-route rate limiting (bounded windows, per-IP sample, payment GET limits) | P1 | M | — | DONE |
| 002  | Batch student read-path queries (remove N+1 in lesson list/detail/summary/continue/prev-next) | P1 | M | — | DONE |
| 003  | Fix placement final-submit lock dead-end after a stale-revision recovery | P1 | S | — | DONE |
| 004  | Repair two vacuous smoke assertions (placement score→level mapping, duplicate topic+level rejection) | P1 | S | — | DONE |
| 005  | Fix server error contracts — progress position-0 500 and placement raw-error leakage | P2 | S | — | DONE |
| 006  | Fix the Caddy 6 MB body cap vs the 64 MB content-import contract | P1 | S | — | DONE |
| 007  | Batch per-row lookups in the staff queue and the admin episode list | P2 | S-M | — | DONE |
| 008  | Onboarding and doc drift — README, .env.example, stale docs | P2 | S | — | DONE |
| 009  | Make the Caddy access-log redaction drill run in CI | P2 | S | — | DONE |
| 010  | Remove dead student routes, previewData, and the unused workbox-window dependency | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 and 002 both edit `server/pb_hooks/lesson_routes.pb.js`, but in
  disjoint regions (rate-limit closures vs. query handlers). Land them in
  separate commits; if one session executes both, run the union of both
  plans' verification suites.
- 004 is a prerequisite for trusting any future server-behavior plan that
  touches placement scoring or the lessons schema: its two scenarios are
  currently vacuous.
- 005's progress-create fix is independent of 002's progress summary/
  continue batching (different handlers).
- Recommended order: 001 → 002 → 003 → 004 → 005 (all independent; smallest
  first if you want fast wins: 003, 004, 005).

## Implemented 2026-08-14 (commit on branch `advisor/five-plans`)

All five plans were executed in one session; the status rows above are DONE.
Documented deviations from the plans as written:

- **Plan 001, scope**: `server/pb_hooks/content_admin_core.pb.js` was
  converted to the shared rate-limit module even though the plan listed it
  out of scope — the plan's premise ("already has the eviction branch") was
  factually wrong (verified: it had no eviction). Behavior preserved (same
  `__fepAdminRate` window, same per-bucket composite keys, same budgets);
  the caller-facing `{status, code, message}` shape is kept.
- **Plan 005, contract**: the plan assumed update-to-0 on an existing
  progress record stays legal (200). Verified during execution that PB 0.39
  rejects 0 on ANY save of a required number field (create AND update), so
  the final contract rejects 0 on both paths with 400 `invalid_position`;
  on update, a stale revision (409) is reported before the 0 rejection. The
  client (`useProgressSave`) drops non-409 errors silently, so the 400 is
  inert in the current UI. Smoke scenarios assert the final contract.
- **Plan 005, smoke**: the S0 per-threshold loop additionally asserts
  `kind === 'level_selection_required'` (reviewer hardening).

## Implemented 2026-08-14 (branch `advisor/plans-006-010`)

Plans 006-010 executed in one follow-up session. Deviations from the plans
as written:

- **Plan 009, scope extension (pre-existing gate fix)**: while wiring the
  Caddy drill into CI it was discovered that the canonical gate had been
  RED on main since the perf slice (PR #4, 2026-08-14 09:25): the PWA
  precache scan in `scripts/project-verify.sh` flagged the public
  post-split `assets/tokens-*.js` chunk name via `/token/i`. The scan is
  now query-precise (matches the SW's own `isProtectedUrl` guard:
  `api/`, `files/`, or a `token=` query param). This was required for the
  drill to run in a green gate; the full gate is green locally again
  (exit 0) and the FEP_REQUIRE_CADDY branch fails fast as designed.
- **Plan 010, scope reduced**: `workbox-window` was NOT removed — the
  plan's death certificate was wrong: `vite-plugin-pwa`'s
  `virtual:pwa-register/react` resolves to it internally, and removing it
  breaks `pnpm build:app` (reproduced, then reverted). The three dead
  route files and `previewData.ts` were removed; `LessonDirection.test.ts`
  was repointed at the live `LessonDetailRoute` + `readingMaxWidth` token
  (contract: LTR isolation + bounded 40rem measure).
- **Plan 008, scope**: `docs/ARCHITECTURE.md` needed no change (the "15
  suites" claim existed only in `docs/TOOLING_SETUP.md`); PLAN.md records
  for Slices 7-9 appended in the established style.
- **Plan 006**: no local caddy binary, so verification is the new static
  test `tests/deploy-config.test.mjs` (wired into the harness lane) plus
  the deploy-time `smoke-prod.sh`; `caddy validate` deferred to a machine
  with caddy.

## Findings considered and rejected (vetted; not planned)

Recorded so nobody re-audits them as new:

- **Full-file audio/receipt reads per request** (lesson_routes `:1223-1340`,
  payment receipt, pronunciation): real cost, but the fix (ranged reads or
  native file serving behind the auth gate) risks the entitlement boundary —
  deferred as a HIGH-risk perf plan; needs the audio-proxy invariants
  preserved.
- **Superuser API publicly reachable via `/api/*`** (Caddyfile `handle /api/*`
  → PB; only `/_/*` is 404'd; docs claim "SSH tunnel only"): real
  decision-drift finding, but the fix (superuser IP allowlist + doc
  correction) is a production-config + operator-input task — planned only
  when deployment gates open.
- **No CSP; PB tokens in localStorage**: documented pre-launch follow-up in
  the Caddyfile itself; MED confidence on exploitability (no XSS found);
  deferred to the launch plan.
- **Caddy 6 MB body cap vs 64 MB content-import multipart**: deploy-blocking
  for the content pipeline, but no production deployment exists yet — plan
  006 on request; requires deciding the package ceiling (Caddyfile-scoped
  cap vs. lowering zip limits).
- **`content_import_core.pb.js` hand-maintained mirror** of shared/
  content-package: L effort, MED risk; parity smoke exists; not worth the
  codegen investment while the pipeline is pre-launch.
- **Entitlement-chain + `checkRate` duplication across hooks** (~25 + ~12
  copies, already drifted error codes): M effort, MED risk; plan 001 removes
  the `checkRate` half; the entitlement half stays a candidate.
- **~40 fixed `waitForTimeout` sleeps in e2e**: real but low urgency;
  conversion churn itself risks flakiness — revisit after launch pressure.
- **p3-s2 API-only tests duplicating smoke coverage**: MED impact (runtime
  cost, layer-contract blur) — deferred; not worth churn before launch.
- **README empty + `.env.example` missing names**: high onboarding value but
  S effort with no verification story; plan 008 on request.
- **Caddy redaction drill skipped when caddy absent** (CI never installs it):
  plan 007 on request (add caddy to the CI backend lane).
- **`verify-affected.mjs` routing gaps** (app changes → no browser lane):
  infra improvement, MED value — deferred.
- **Biome excludes `server/pb_hooks`** and inert `eslint-disable` comments:
  real, but re-enabling Biome on goja-idiom files is MED-risk and could
  churn huge diffs; a lint-only lane is plan 009 on request.
- **PLAN.md stops at Slice 6 / TOOLING_SETUP says "15 suites" / PRODUCT.md
  criteria all unchecked**: doc drift, S effort — fold into plan 008 on
  request.
- **Dead code** (`app/routes/DashboardRoute|LessonsRoute|LessonDemoRoute` +
  `previewData` + unused `workbox-window`): S effort, LOW risk — trivial for
  any executor; included in the table below, plan on request.
- **Formatters/CEFR-order duplication** (app/admin/shared, 5 CEFR lists):
  S-M effort consolidation — deferred.
- **Placement question seeding duplicated in ~10 suites**: M effort;
  plan 004 already improves the highest-value instance.
- **No `pnpm audit` in CI**: add `pnpm audit --prod --audit-level high` to
  the static lane — S effort, plan on request (TECH-11; run once locally
  first).
- **TypeScript 7.0.2 (native compiler)**: informational only — not a
  finding, no action. Rejected as "not worth doing".
- **Direction items** (launch gates, telemetry endpoint, background audio,
  reviewed placement questions, landing redesign): product decisions, not
  plans — see the audit report's direction section; each needs the
  maintainer's input (keystore/VPS/device/EFL review).

## Full vetted findings table (audit report)

Security: sample-route global rate budget (→001) · unbounded rate-limit maps
(→001) · unthrottled payment GETs (→001) · placement raw-error echo (→005) ·
superuser API exposure (rejected above) · Caddy import body cap (rejected
above) · no CSP (rejected above) · telemetry redaction gaps (paths/spaced
phones in `redact.ts` — S fix, unplanned).

Correctness: placement submit-lock dead-end (→003) · progress position-0 500
(→005).

Performance: lesson list N+1 (→002) · summary/continue N+1 (→002) ·
prev/next full-scan (→002) · staff queue full-load + per-row lookups
(unplanned, M) · library duplicate progress fetch (unplanned, S) · audio
full-file reads (rejected above) · LessonDetailRoute un-memoized per-render
work (unplanned, S).

Tests: vacuous score-mapping loop (→004) · swallowed dup-topic assertion
(→004) · hero-artwork route zero coverage (unplanned, S) · fixed sleeps
(rejected above) · p3-s2 API-only tests (rejected above) · caddy drill skip
(rejected above).

DX/docs: empty README + env names (rejected above) · Biome excludes hooks +
inert eslint-disable (rejected above) · affected-routing gaps (rejected
above) · doc drift 15/16 + PRODUCT.md (rejected above) · PLAN.md stale
(rejected above).

Tech debt: entitlement/checkRate duplication (rejected above) ·
content_import mirror (rejected above) · dead routes + workbox-window
(rejected above) · formatter/CEFR duplication (rejected above) · seeding
duplication (rejected above) · 4 error-mapping systems (unplanned, M) · no
audit in CI (rejected above).
